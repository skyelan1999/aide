# 源码开发默认 aide:local；导入 Release 后改为已镜像 tag，如 aide:0.1.10.2-RC1
# 离线/空气 gap 机：docker load 后把 AIDE_IMAGE 设为导入的 tag，只用 start-image 启动，绝不跑 start（会触发 build 拉取基础镜像）
AIDE_IMAGE=aide:local
# 主机端口，只绑定本机
AIDE_PORT=8097
# 唯一 Compose 配置入口；configure-local-root.py 可切换 macOS 共享根组合
COMPOSE_FILE=compose.yaml
# 容器工作目录，可改成其他本地项目的绝对路径
AIDE_WORKSPACE=.
# 辅助资料目录，只读挂载；默认使用已有 Harness 源码库
AIDE_CONTEXT=../Harness
# 宿主机本地目录根（工作空间配置面板按电脑真实路径浏览/选择），默认整个用户主目录，可读写
AIDE_LOCAL_ROOT=$HOME
# 模型接入。留空时在浏览器"模型设置"中配置；不会自动调用或产生模型费用。
# 联网默认公有云；离线/空气 gap 时填局域网内可访问的模型服务地址（本地 vLLM / 内网网关），切勿指向公网。
AI_BASE_URL=https://api.deepseek.com
AI_MODEL=
AI_API_KEY=
# 在线搜索端点（SearXNG 兼容 JSON）。联网部署填公开/自建实例以恢复 DuckDuckGo 在线搜索能力；
# 离线/空气 gap 部署必须留空——web_search 将优雅降级为"请用 search_text 搜本地"，不发起任何外联。
AIDE_WEBSEARCH_URL=

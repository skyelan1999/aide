# aide 交接文档（2026-09-26 · v0.1.11.0 RC3 → 交接给 GPT）

> 用途：豆包（Doubao）侧多智能体团队已停止工作，剩余开发/测试/收口由 GPT 接手。本文自包含，按“先核对、再动手”的顺序使用。
> 安全提示：本文**不含任何密码、访问令牌、API Key、私钥**；这些只存在于运行环境与数据卷，需用户在真机输入。历史操作细节见仓库根 `HANDOVER.md`（档案，止于 0.1.10.x）。

---

## 0. 接手须知（TL;DR）

- 产品：**aide = AI + IDE**，自托管、单容器、可离线的本地 AI 专业工作台。
- 技术栈：**Go 后端（go:embed 内嵌前端）+ 原生 JS Web 前端（无构建框架）+ Docker Compose**；容器内含 Go / Python 3.12 / Node 24 运行时。
- 当前工作分支：`feature/permission-panel`；**HEAD = `c978fd5`，已与远程 `origin/feature/permission-panel` 完全一致（全部已 push）**；工作树干净。
- 容器 `aide-aide-1` 正在运行、healthy，本地镜像 `aide:local`，端口 `127.0.0.1:8097 → 容器 8080`，运行版本 v0.1.11.0-RC3。
- **最高优先级未完成项 = 主会话点麦克风后“小秘语音框不出现”（#47 相关，回归）**，根因与修法见 §8 P0。
- 硬约束：**未获用户许可，不合 main、不正式 release、不打/移 tag**；详见 §3。

---

## 1. 项目定位与技术栈

- 定位：让人更专注专业工作的本地 AI 工作空间；强调**隐私自托管、离线可用、可扩展插件、多智能体编排、桌面级文件处理**。
- 后端：Go（约 92 个 `internal/server/*.go`），通过 `go:embed` 将 `internal/server/web/` 原生前端打包进单一二进制；无外部前端构建链。
- 前端：原生 HTML/CSS/JS（`internal/server/web/`），i18n 显式本地化（`i18n.js` + `locales/en.js`），不做全 DOM 文本替换。
- 编排：Docker Compose（`compose.yaml`，另有 `compose.dev.yaml`、`compose.macos-root.yaml`）；`Dockerfile` 构建期通过 ldflags 注入版本/commit。
- 容器运行时：Go 1.26、Python 3.12（含 python-docx / openpyxl / python-pptx / ezdxf / pypdf）、Node.js 24（含 node:sqlite）；**不含 LibreOffice/soffice**（体积与离线考虑，已明确不打进主镜像）。
- 远程仓库（SSH）：`git@github.com:skyelan1999/aide.git`；受保护主线为 `main`（无 master）。

---

## 2. 当前状态快照（权威，已核对）

| 项 | 值 |
|---|---|
| 工作分支 | `feature/permission-panel` |
| HEAD | `c978fd5`（i18n: wrap WebAuthn credential name with t() #26） |
| 工作树 | 干净（`git status --short` 为空） |
| 本地 vs 远程 | 一致（`origin/feature/permission-panel..HEAD` 为空，全部已 push） |
| 相关版本 tag | `v0.1.11.0-RC1`、`v0.1.11.0-RC3` |
| RC3 tag 指向 | 较早提交（约 `2d5e368`），**早于英文化**；最终代码 c978fd5 比 RC3 tag 新 |
| 运行容器 | `aide-aide-1` Up、healthy，镜像 `aide:local` |
| 端口 | `127.0.0.1:8097 → 8080`；健康检查 `/healthz` |
| 运行版本 | v0.1.11.0-RC3、commit c978fd5、integrity=ok |

> 版本注入要点：构建时必须传 build-arg，否则 `/api/config` 会报 `buildVersion=dev`、`buildCommit=unknown`。`scripts/aide.sh` 会导出 `AIDE_VERSION`/`AIDE_COMMIT`；手动构建见 §4。
>
> **应用代码 vs 文档提交**：容器运行的应用代码 = **0.1.11.0-RC3 / c978fd5（已 push）**。本交接文档（HANDOFF）与 HANDOVER 顶部指针，是在 c978fd5 之上的**文档提交（仅 .md、无代码变更）**，因此 `git log` 顶部可能是文档 commit、本地领先远程；这不改变运行中的应用版本。是否把文档 commit 也 push 到 `feature/permission-panel`，由用户决定（不合 main）。

---

## 3. 硬约束（必须遵守）

1. **未获用户明确许可：不合 main、不正式发布、不打新 tag、不移动现有 tag。** 所有实验性改动只在 `feature/permission-panel` 一条工作线上；可在分支上任意提交、push 到远程分支。
2. 一个功能不再单独开分支：**一条线串行推进、在上一个改动基础上继续验证**，便于最终回归。
3. 一切结论以**真机 / 日志 / 截图 / 可复现命令**为准；**禁止把“计划/预估”写成 PASS**；关键路径至少测 3 次、换条件、覆盖上下游。
4. UI 变更必须实际打开页面验收；没有浏览器/真机能力时记录 `NOT_RUN` 并交接，不得假装通过。
5. 安全：密码只存 Argon2id 哈希、不明文回显；加密用 AES-256-GCM；做输入校验；私密数据不进 Git、不贴公开日志。
6. UI 审美：使用主题 CSS 变量（不硬编码蓝/红），风险色用 `var(--danger)`；不显示“危险操作”字样；左下角按钮不用蓝调；遵循大厂级精致度。
7. 会话输入默认**排队模式**；工具调用轮数默认 **60**（设置中可配，范围约 5–200）。
8. 不自行授权付费调用、推送、发布或停生产服务；流程要求不替代用户当前指令。
9. 清理只删除确认可再生的垃圾；**禁止 `git clean -fdx`**；未跟踪文件不是垃圾。

---

## 4. 运行 / 构建 / 测试

### 日常启动（macOS）
- 双击 `start.command` → `scripts/aide.sh` → Docker Compose；端口默认 8097，配置以 `.env` 为准（`AIDE_PORT`、`COMPOSE_FILE`）。
- 其他平台入口（暂未在其上验证，需在文档中标注局限）：`start.sh`（Linux）、`start.bat` / `start.ps1`（Windows）。

### 手动构建（注入版本，避免 dev/unknown）
```bash
# 路径含空格，注意引号
cd "<repo-root>"
export AIDE_VERSION="$(bash scripts/version.sh show)"     # 当前应为 0.1.11.0-RC3
export AIDE_COMMIT="$(git rev-parse --short HEAD)"
docker compose build aide
docker compose up -d
```

### 测试
- Go：`go test ./...`；竞态/全量：`go test -race -count=1 ./... && go vet`。
- 路由脚本（仓库约定）：
  - `python3 scripts/agent-route.py check`
  - `python3 scripts/agent-route.py verify quick`
  - `python3 scripts/agent-route.py verify full`（一次性容器内跑 race/vet，需本地 aide:local 镜像）
  - `python3 scripts/agent-route.py audit`
- 前端/无头 UI：`scripts/ui_stream_check.cjs`、`scripts/ui_queue_check.cjs`（需 playwright-core + CHROME_PATH，缺依赖自动 SKIP）；`scripts/test_i18n.cjs`、`scripts/test_stream_url.cjs`。
- 离线发布：`scripts/docker-release.sh`（产出离线 tar + SHA256SUMS；`RUN_TESTS=1` 可在构建期跑测试）；`scripts/prebuild-wheels.sh` 用于 air-gap 预构建 wheel。
- 版本规则统一用 `scripts/version.sh`，**不另造版本号/发布脚本**。

### 鉴权（自测时）
- 访问令牌在容器内 `/data/auth/access-token`（命名卷持久化）；请求头 `Authorization: Bearer <token>`；自动登录 `https://localhost:8097/#token=<token>`。
- 静态资源（含 index.html、app.js 等）统一返回 `Cache-Control: no-store`：浏览器不缓存、每次取最新，**不存在“修复因浏览器缓存不生效”的系统性问题**。若“改了没生效”，优先核对**容器是否真的用新镜像 force-recreate**，以 `/api/config` 的 `buildCommit` 为权威。

---

## 5. 系统架构概览

### 5.1 分层
- **接入层**：单一 Go HTTP 服务（容器 8080），TLS（https）+ 统一身份认证；本地 8097 端口映射。
- **会话/编排层（Go）**：会话与 run 生命周期、SSE 事件总线、工具循环（toolLoop）、队列/插话、多智能体调度（spawn_subagent / lead agent）、AI 工作流四阶段。
- **能力层（容器内多运行时 + 插件）**：
  - 内置工具：文件读写、shell 执行（容器沙箱、命令黑名单）、网页搜索、语义搜索、记忆等。
  - 插件（DSH 形态：默认导出含 `apply(ctx)` 的对象；见 `docs/plugin-protocol.md`）。
- **数据层**：命名卷 `/data`（会话、认证、密钥/凭证、来源登记等）、`/home/aide`；宿主挂载 `/workspace`(rw)、`/context`(ro)、`/local`(rw，宿主根)。
- **表现层**：go:embed 原生 Web（`internal/server/web`）；各类文件查看器。

### 5.2 关键机制 / 协议 / 算法
- **流式输出**：SSE（`GET /api/sessions/{id}/runs/{run}/events`，事件 step/delta/tool/status/done）；前端 delta 用 requestAnimationFrame 批量渲染；流断开由轮询兜底重连。
- **多智能体**：`spawn_subagent` 产生“子会话”，挂载在主会话下；子会话完成自动归档（归档中仍为子会话形式，关联主会话；主会话归档后可关联）。自动模式下由 **lead agent** 统一调度多个子 agent，初始会话负责与用户沟通并最终讲解核对。
- **AI 工作流**：对话模式（无阶段按钮）与 AI 工作流模式（彩色动画的四阶段：**需求 → 设计 → 实施 → 验证**，顺序固定）。
  - 需求：强提示词触发，在辅助资料/系统文档自动创建 requirement analysis 文档，AI 对齐并分配唯一编号、做差异记录。
  - 设计：基于需求文档输出开发流程、依赖、架构，动态更新并记录变更，含流程图与文字。
  - 实施：在工作路径生成/修改实际内容。
  - 验证：依据文档编写测试用例/方案、自动化测试、输出测试报告。
  - 未手动选阶段 = 自动模式（多子 agent + lead 调度）。
  - 每个阶段不清楚时必须逐步向用户确认（参考 Codex 的确认节奏），未确认不进入下一步。
- **自动路由**：对话模式默认“默认”；AI 工作流模式由初始 agent 根据需求与研发特性**核对真实参数**后选择配置传给子 agent；参数不具备时提示用户应配置项，手动配好后再启动。
- **安全/加密**：TLS；密码 Argon2id；敏感数据 AES-256-GCM；API Key vault；WebAuthn / Touch ID（macOS）；完整性校验与自愈（见 #31）。
- **记忆与性格**：缓存目录中的记忆系统；aide 与小秘两套可演化性格（提示词随使用自主微调以节约 token）；小秘记忆独立、aide 记忆可被小秘读取，小秘可获取 aide 流式输出。
- **搜索**：网页搜索（DDG HTML 抓取、SearXNG 兜底，无需百度类 API）；文件文本查找支持**关键字 + 向量化**两种，模型自行决定；会话搜索包含归档。

### 5.3 文件查看器（`internal/server/web/vendor` + app.js）
- Markdown / Mermaid（流程图）、代码高亮（highlight.js，多语言）、JSON 着色；
- Word（docx-preview + JSZip）、PDF（PDF.js）、图片预览；
- STL（three.js + STLLoader + OrbitControls）、DXF（dxf-parser + SVG 内联渲染）、drawio（已 vendor，支持查看/编辑/文件关联）。
- 文件交互：单击打开、双击新标签页打开、双击文件名重命名（点击别处/勾选自动保存）；项目文件在新文件生成后自动刷新。

### 5.4 优势
- 单容器、自托管、可离线（air-gap 发布 tar，保密环境无需外网）；
- 隐私优先（加密、密钥不回传、本地处理）；
- 插件化可扩展（DSH 形态）+ 多 agent 编排 + 四阶段工作流；
- 桌面级多格式文件查看/批注；go:embed 单一二进制、部署简单。

---

## 6. 数据与目录分层（接手后需复核并落文档）

- 跟随**项目缓存**（工作区 `.cache/`、来源登记 sources.json、记忆等）vs 放进**源码路径**（plugins、docs、配置默认值）要有清晰边界；小秘加密会话属敏感缓存，不应进源码/ Git。
- 容器挂载：
  - `/context`：内置只读来源（Harness），**常驻、不可移除、不被 Docs.Path 覆盖**；
  - `/workspace`：工作目录（可写）；
  - `/local`：宿主根（可写，工作区如 `/local/Users/skyelan/debug`）；
  - `/data`、`/home/aide`：命名卷。
- 核心组件完整性：自动校验 + 恢复（#31，提案 `proposals/data-integrity/data-layering-and-self-healing.html` 已批准并实施，需复核现状）。
- 工作目录语义不一致问题（/workspace vs 用户工作区 /local）见 #61 与 `docs/reviews/2026-09-26/workspace-semantics-design.md`，**方案待用户确认**。

---

## 7. 任务状态总览（#1–#64）

> 说明：#1–#28、#62 已完成。#29–#61、#63 多为 in_progress，#64 pending。**注意：部分 in_progress 任务的核心代码已从 git log / vendor 可见落地，标记 in_progress 主因是“未最终真机验收/收尾”**；GPT 接手后应先逐项真机核对真实状态，再决定继续开发还是仅验收，禁止凭标签假设。

### 已完成（completed）
#1–#28（归档回新会话、插话/排队、权限面板、四阶段按钮、模型卡片、遮罩关闭、推理强度、策略菜单、轨迹面板、语音弹框、STL/图片/drawio、性格系统、讲解稿与上下文注入、讲解勾选、无障碍、历史记录、勾选自动保存、性格持久化、停止中断、登录后锁定、配置导入导出、文档 review、Web 英文化、版本收口、轨迹控件重排）；**#62 小秘会话闭环**。

### 进行中 / 待收口（in_progress，按主题归并）
- 安全与合规：#29（安全加固/欧盟合规）、#43（TLS 收口：WebAuthn https origins、单端口 http→https、证书目录）、#38（SSH 私钥路径/粘贴双输入 + 凭据加密）。
- 数据/配置：#31（目录分层 + 完整性自愈）、#32（配置导入导出跨版本默认值回填 + 迁移钩子）、#40（恢复出厂设置，范围可选 + 自动备份）。
- 小秘能力：#30（会话编号 + 小秘置顶系统会话 + 跨会话跟进推送）、#33（小秘自我意识统一提示词）、#35（记忆单向可见 + 流式桥接）、#36（个性化音色克隆 + 声音推断性格）、#41（发送自判插队/排队）、#42（音色“没差”：edge 误降级 + UX）、#44（多引擎 TTS：edge/sherpa-onnx 离线/浏览器 + 状态可见 + 降级）、#45（硬停止 + 概括忠实度/详细度）、#47（小秘面板薄条 UI，**当前含 P0 回归**）。
- 性能/可靠性：#46（启动提速：默认构建不跑全量 go test、源码未变 no-build）、#51（思考转圈：高频流式重建导致动画重启）、#52（max_tokens 截断：未完成 tool call 自动续写，DXF 无法生成）、#55（刷新/重开误锁屏：锁定状态后端持久化）、#60（会话列表 SSE 自动刷新：子会话挂载/归档/标题变更）。
- 调试/设置 UI：#48（外部 AI 调试：降噪自刷、调用记录表格化/筛选、日志下载、UI 重做）、#49（设置页通用 number 渲染器 + 渲染健壮性 + nav 滚动）、#50（模型占比饼图 labelLine 引导线）。
- 文件/来源：#53（辅助资料多来源，核心已修待验收）、#54（PDF 内联查看 PDF.js）、#57（DXF 矢量渲染器）、#58（取消文本 256KiB 限制）、#59（编辑器文件操作归并顶部栏）、#63（Word doc/docx 查看 + 批注，侧车持久化、模型可读）。
- 调研：#61（工作目录语义，方案待确认）。
- 集成：#56（两个独立测试 agent 全面回归 → 修复 → 真机验收 → 正式 tag/push）。

### 待开始（pending）
- #64：代码文件语法高亮（.py 优先；vendor highlight.js 已就位，需接入编辑器/查看并验收）。

---

## 8. 剩余工作清单（交给 GPT，含验收口径）

### P0 — 修复“主会话点麦克风，小秘语音框不出现”（#47，回归；已完整定位）
- **真机现象**：在主会话（新建会话空白页）点麦克风，麦克风变红（语音在跑），但小秘语音“框”完全不出现，转写/状态无处可看。
- **根因**：`internal/server/web/app.js` 中 `#voice-btn` 的 onclick（约 4978–4985 行，行号会漂移，动手前重新定位）按会话 kind 分流：
  - 小秘会话 → `voiceStart()`；**主会话 → `dictationStart()`**。
  - `dictationStart()`（约 4988）只做纯 Web Speech 听写、文字填进 `#prompt` 让人手动发：**不显示 `#voice-panel`、不做 AI 噪声甄别、不自动断句发送**。这偏离用户原始诉求，属回归。
- **用户明确诉求（验收基准）**：
  1. 点麦克风即唤起**小秘语音管线**，主会话与小秘会话都要显示小秘语音“框”；
  2. 框内实时采集、**自动动态断句**、**AI 甄别**（要传达的意图 vs 背景噪声/刷视频/与他人闲聊；闲聊自动退下）；
  3. 断句后**直接发送**（不放进输入框手动发），发送沿用 #41（紧急/中止插队，新任务排队）；
  4. 框内嵌在主要聊天框区域、**不挡下面内容**，做成贴输入区/会话区上方的薄条，可启动/停止。
- **修复要求**：
  1. voice-btn onclick 不再把主会话降级为 dictation，主会话也走 `voiceStart`；若保留“纯听写”，做成设置里的语音模式开关（默认小秘助理）。
  2. `#voice-panel` 显示时机提前/独立于麦克风成功：点击后先弹框显示“正在请求麦克风…”，再 `await voiceOpenMicStream()`；失败/拒绝给明确提示与重试，避免麦克风慢/失败时框完全不出现。
  3. `#voice-panel` UI 重做（薄条、不挡内容、主题 CSS 变量、不硬编码颜色）：展示小秘名字、聆听/停止状态、当前断句 interim、每条甄别结果（已发送/已忽略）、排队状态；停止即收起。
  4. 自动断句 + AI 甄别 + 直接发送链路（`voiceExtractSentences` / analyze / `voiceDrainQueue`）在主会话同样生效。
  5. `node --check`；补充/更新前端测试。
- **验收**：
  - 无真实麦克风时用 mock（stub `getUserMedia` / `webkitSpeechRecognition`）验证：点击后面板立即显示、状态与断句/甄别渲染、发送触发（#41 排队/插队）、停止后面板隐藏；附截图。
  - **真实麦克风的实时甄别与发送 = NOT_RUN，需用户真机验收。**
  - 修复后 commit；**push 与否等用户统一安排（先不 push）。**

### P1 — RC3 收口前必须真机逐项核对/回归（#56）
- 由两个独立视角做全面测试（建议：① 代码层 go test -race/vet/构建/接口/插件；② 桌面仿真层真机浏览器 GUI E2E）。
- 对所有“代码已落地待验收”的 in_progress 任务逐项真机核对，重点（用户反复点过名）：
  - 文件查看器：**STL、图片、drawio、DXF、PDF、Word(docx)、JSON、代码(.py)、Markdown/Mermaid** 逐一打开核对渲染；不支持的类型“保存”按钮应禁用。
  - 辅助资料多来源（#53）：内置 /context 常驻置顶、不可移除、可新增/切换/pin；工作目录 RW / 辅助资料 RO 正确。
  - 会话：归档后回新会话；子会话挂载/归档显示；会话列表与标题经 SSE 自动刷新（不靠手动转圈箭头重复消耗 token）；会话搜索含归档。
  - 中断/截断：提前停止或工具轮数超限，**已流式输出的内容必须保留**（仅标记会话被打断），不得显示“未返回任何内容”；max_tokens 截断自动续写（#52）。
  - 锁屏联动：主界面未锁定→从界面不应锁定；主界面锁定→从界面跟随；从界面解锁不解主界面；刷新后立即锁定不应误显示（#55）；每个密码输入处都可用指纹；Touch ID 未注册时指纹按钮置灰。
  - 设置页：固定大小、切换选项不跳动；自定义配置可单个/全部删除（全部删除需确认）；1M 等值可正确输入；工具轮数可配；恢复出厂设置入口（标题写“恢复出厂”，不写“危险操作”）。
  - 轨迹：进入工具调用后时间线仍可看；历史/调用记录为可切换视图；导出按钮文字居中；调用分析可 filter、可看子 agent 何时动作、可下载日志。
  - 上下文：不同颜色标注各功能上下文消耗，hover 高亮并显示详情；消耗统计下含“不同模型 token 占比”饼图（labelLine 连接标签）。
  - 消息操作：复制、重试、继续、好的回答、有问题的回答（好/坏记入记忆用于再训练）、创建分支；按钮做成同色纯按钮（用户后续又说先不改按钮，以最新指令为准——**接手时与用户确认按钮现状再动**）。
- 关键路径至少 3 次、换条件、覆盖上下游，避免偶然；缺陷修复后对缺陷及相邻功能回归重测。

### P2 — 能力补全与文档
- #64 代码 .py 语法高亮接入；#57 DXF 渲染真机确认；#54 PDF.js；#58 取消 256KiB；#59 顶部栏归并；#60 SSE 自动刷新；#63 Word 查看 + comments（并让 aide 可读批注、知道改哪里；办公插件为开源免费方案）。
- 小秘：#33 自我意识、#34 性格自主微调落地、#35 记忆单向可见/流式桥接、#36 音色克隆、#42/#44 TTS 引擎与音色差异化（用户反馈“音色没差/太机械”，需真实可辨、更自然）、#30 置顶系统会话与跨会话推送。
- 插件：#37 通讯套件（SSH/TCP/UDP/串口 监听与模拟）、#39 SQLite（node:sqlite，插件目录已存在，需真机验收）。
- 外部 AI 调试接口（#48，开关在设置→无障碍，控制是否允许外部 AI 接入；无浏览器也可远程获知状态/调试）。
- 配置：#32 跨大版本导入默认值回填；#40 恢复出厂范围；配置全部导入/导出引导。
- 启动/离线：#46 启动提速；`start.command` 导入 Docker 后不再从外网拉取（air-gap），做好 docker release，保密环境可用。
- 平台：完善并文档化 Linux/Windows 启动方式与局限（当前不在其上验证）。
- **文档（用户反复强调）**：所有文档随代码同步，**中英双语**，该做图做图；review 全仓库目录下文档、更正不正确内容。

---

## 9. NOT_RUN / 需用户真机配合
- Touch ID 按压解锁（含指纹注册流程）、指纹在各密码门的真实授权。
- 真实麦克风录音、实时断句/甄别/发送。
- sherpa-onnx 离线 TTS（模型未下载）。
- xlsx / pptx 附件、64MiB 大文件的桌面实测。
- 苹果键盘指纹键解锁（# 解锁支持 Touch ID）。
> 这些项不得标记为通过；需用户在真机操作并保留截图/证据。

---

## 10. 待用户决策（不要擅自决定）
1. 是否**升 RC4** 并在最终真机终审后重新打 tag（使 tag 精确对应含英文化的最终代码 c978fd5；当前 RC3 tag 指向更早提交）。
2. 各修复完成后是否 push 到远程 `feature/permission-panel`（不合 main）。
3. 消息操作按钮（复制/重试/继续/好/坏/分支）的最终样式与去留（用户指令有反复，需确认）。
4. #61 工作目录语义（/workspace vs /local）方案选择。
5. NOT_RUN 真机项的时间安排。

---

## 11. 文档与产物地图
- 入口/约定：`AGENTS.md`、`README.md` / `README.en.md`、`version.md`、`HANDOVER.md`（历史）、`web-i18n-refactor-plan.md`。
- 核心文档：`docs/architecture.md`、`docs/architecture-overview.html`、`docs/installation.md`、`docs/user-guide.md`、`docs/customization.md`、`docs/plugin-protocol.md`、`docs/workspace-paths.md`、`docs/debug-api.md`、`docs/verification.md`、`docs/PRD.md`、`docs/prd/`。
- 文档分区：`docs/architecture`、`docs/design`、`docs/security`、`docs/plugins`、`docs/releases`、`docs/reviews/`（含 `2026-09-26/`）、`docs/en/`（英文）、`docs/agent/`（WORKFLOW、router.json、tasks）。
- 提案：`proposals/data-integrity/`（已批准实施）、`proposals/ipad/`（**已否决 REJECTED，不实施**）。
- 插件：`plugins/`（skill、goal、plan、todo、feedback、subagent、terminal、workflow、environment-guide、sqlite、comm-tcp/udp/serial/ssh；含 `registry.json`、`surface.json`、各 `manifest.json`）。
- 脚本：`scripts/`（aide.sh、version.sh、docker-release.sh、prebuild-wheels.sh、agent-route.py、各类 test/check）。
- 前端：`internal/server/web/`（app.js、i18n.js、index.html、settings-schema.json、style.css、macos.css、settings-init.js、lock-cluster.js、locales/en.js、themes/、vendor/）。

---

## 12. 已知边界与平台局限（需在文档保持最新）
- 远程 SSH 工作区的 run_shell 自动执行未打通。
- 主镜像不含 LibreOffice；`.doc` 维持转 `.docx` 提示（不把 LibreOffice 打进主镜像）。
- iPad 客户端已否决，不实施。
- Linux/Windows 启动方式需“看起来可用”并写明局限，暂不在其上验证。
- 每轮工具调用上限可配（默认 60）；多轮排队/插话会消耗轮次；任务结束瞬间到达的排队消息可能不获回答；steer 中间回答折叠进最终答案。
- no-store 静态资源策略：每次刷新重新下载所有 vendor（localhost 下可接受），如后续优化需改为 index no-store + 带内容 hash 的 vendor 长缓存。

---

### 交接状态（截至 2026-09-26）
- 已停止：全部子智能体（统筹及开发/调研/测试 agent）；每小时自动汇报定时任务已删除。
- 已完成并 push（不合 main）：截至 c978fd5 的全部改动（含 Web 英文化、#62 小秘闭环、#53 内置来源、#45 调用登记等）。
- 未完成首位：§8 P0 主会话小秘语音框回归（根因/修法/验收已写明）。
- 运行中服务：`aide-aide-1` healthy（v0.1.11.0-RC3）；发布/回滚状态：**未正式 release、RC3 tag 与最终代码不一致，待用户决定 RC4**。

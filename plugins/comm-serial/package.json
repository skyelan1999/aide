{
  "name": "aide-comm-serial",
  "version": "0.1.0",
  "private": true,
  "description": "aide daemon plugin: serial port I/O via serialport, plus virtual serial pairs for testing/simulating devices",
  "main": "index.js",
  "dependencies": {
    "serialport": "^12.0.0"
  }
}

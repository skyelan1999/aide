# aide 产品需求文档（PRD）

本文是 aide 的**需求基线**。每次开工之前先读一遍第 0 节和第 10 节，确认"这次要做的事"在范围内、状态标记与代码一致，再动代码。

| 项目 | 值 |
| --- | --- |
| 文档版本 | v1.2 |
| 建立日期 | 2026-09-21 |
| 最近修订 | 2026-09-21（v1.2：依据用户裁决界定 **NFR-12 适用范围为明色主题**；新增 R-13 暗色既有低对比边界、LIM-20 令牌数量基线、P2 修复路线，见第 11 节）<br>2026-09-21（v1.1：新增主题与配色需求 FR-49 ~ FR-57、NFR-12 ~ NFR-17、LIM-15 ~ LIM-19、OOS-12） |
| 对应源码基线 | `fbe45b2`（文档基线）<br>功能基线 `4e0da10`（本次主题改造前，未变动） |
| 运行容器 | `aide-aide-1` / `aide:local` / healthy / `127.0.0.1:8097` |
| 上游文档 | [HANDOVER.md](../HANDOVER.md)（交接与运维）、[docs/architecture.md](../docs/architecture.md)（架构）、[docs/verification.md](../docs/verification.md)（验证记录） |
| 维护约定 | 需求变更先改本文，再改代码；状态标记必须与实测结果一致 |

---

## 0. 如何使用本文档

### 0.1 开发前核对流程（每次开工必做）

1. **读状态**：`git log -3 --oneline`、`git status --short`，确认基线没被动过、工作区干净。
2. **读本文第 4 节**：找到本次要做的 FR 编号，确认它的当前状态是"未实现"或"已实现·未验收"，而不是被误标为已完成。
3. **读本文第 3.2 节**：确认这件事**不在非目标列表**里。在非目标里的事情，必须先改需求再动手。
4. **读本文第 6 节**：确认方案没有突破量化限制（文件大小、超时、并发、挂载只读等），否则要连带更新限制表。
5. **收尾**：改完代码后回来更新第 7 节验收状态矩阵、第 11 节变更记录；有新的边界限制就同步第 6.5 节。

### 0.2 需求编号规则

- `FR-xx`：功能需求（Functional Requirement）
- `NFR-xx`：非功能需求（Non-Functional Requirement）
- `OOS-xx`：明确排除在范围外的项（Out of Scope）
- `LIM-xx`：硬性量化限制

编号一旦分配不再复用。废弃的需求标为"已废弃"并写明原因，不删行。

### 0.3 状态标记含义

| 标记 | 含义 | 可以据此做什么 |
| --- | --- | --- |
| `已实现·已验证` | 代码已写，且有测试或实测证据 | 可以依赖，但改动后要重跑对应验证 |
| `已实现·未验收` | 代码已写，只跑过模拟/本地受控验证，真实场景未验 | **不能对外宣称可用**，不能当成已完成 |
| `未实现` | 代码里没有 | 需要新开发 |
| `部分实现` | 有主体，缺关键环节 | 需要补齐，并在备注写明缺什么 |

> 关键纪律：**"模型说已完成"不是证据**。只有工具执行输出、测试结果、主机侧实际读取才算证据。模拟 API 通过 ≠ 真实模型通过。

### 0.4 分支与合并约定（用户要求，必须遵守）

1. **每个新功能一个独立分支**：开工前从 `main` 检出功能分支，命名建议 `feat/<功能名>`；**禁止直接在 `main` 上开发新功能**。
2. **验证成功后才合并**：功能分支上的实现必须通过对应验证（后端测试 / 模拟 API / 浏览器实测，并同步更新本文第 7 节状态矩阵）之后，才能合并回 `main`；未经验证的代码不得进入 `main`。
3. **历史例外**：2026-09-21 之前开始的主题切换工作（当时以未提交改动存在于工作区）经用户确认留在 `main` 继续收尾，不溯及既往；该例外不构成新惯例。

---

## 1. 项目定位

### 1.1 一句话定义

aide 是一个**运行在本地 Docker 中、以浏览器为界面的 AI 开发工作台**：Go 单服务后端，可读写挂载本地项目目录，提供会话对话、文件编辑、容器命令执行，以及"规划 → 文件提案 → 审查 → 人工应用"的三阶段 AI 工作流。

### 1.2 背景与来源

目标是复刻 DeepSeek Harness（DSH）的使用体验（会话、模型适配、工具分层、工作流），但**代码完全独立实现**，不引入 Cordis 插件运行时。DSH 的源码与文档作为 `/context` 只读参考资料挂载，仅用于理解架构。

### 1.3 设计原则（改动时不可违背）

| 编号 | 原则 | 含义 |
| --- | --- | --- |
| P1 | **不伪造能力** | 未配置模型时不给假回答；未运行的命令不宣称跑过；未验证的结论标注未验证 |
| P2 | **不自动执行破坏性动作** | 模型生成的命令只展示、不自动运行；文件改动必须用户点"应用"才落盘 |
| P3 | **附件即全部上下文** | 只有用户显式附加的文件才提交给模型；未附加的现有文件不可被提案覆盖 |
| P4 | **不可信输入隔离** | 附件内容、历史模型输出一律视为"资料"，不是指令；只有用户任务定义任务 |
| P5 | **本地单用户** | 只绑定 `127.0.0.1`，随机令牌 + 同源校验；不做多用户授权 |
| P6 | **数据可迁移** | 源码走 Git、镜像走归档、状态走命名卷，三者独立备份 |

---

## 2. 目标用户与使用场景

### 2.1 目标用户

单一可信用户（本机开发者），在自己的 Mac 上管理多个本地代码项目。

### 2.2 典型场景

| 编号 | 场景 | 期望结果 |
| --- | --- | --- |
| S1 | 让 AI 读懂一个陌生项目 | 附加若干源码文件 → 对话模式提问架构与模块职责 |
| S2 | 让 AI 产出一份可审阅的改动方案 | 工作流模式 → 得到规划、JSON 文件提案、审查意见 |
| S3 | 把方案落到磁盘 | 展开 diff、确认无误后点击应用，主机文件同步更新 |
| S4 | 改完立刻验证 | 把建议命令填进命令面板，手动运行看输出与退出码 |
| S5 | 快速改一个小文件 | 在右侧文件面板打开编辑器，改完保存，哈希校验防冲突 |

---

## 3. 范围

### 3.1 本期范围（In Scope）

浏览器工作台界面（含**界面主题与配色方案**，FR-49 ~ FR-57）、会话与历史、双根目录文件浏览/编辑/新建、只读辅助资料挂载、容器命令面板、模型配置、三阶段 AI 工作流、文件提案人工应用、Docker 化部署与镜像归档、访问令牌与同源校验。

### 3.2 明确的非目标（Out of Scope）

以下项**不在本期范围内**，做之前必须先改需求文档。

| 编号 | 非目标 | 说明 |
| --- | --- | --- |
| OOS-01 | 多用户与权限体系 | 明确面向单用户本地工作台 |
| OOS-02 | 远程部署 / 公网暴露 | 端口只绑 `127.0.0.1`；不得暴露给不可信用户 |
| OOS-03 | Cordis 插件兼容 | DSH 仅作架构参考 |
| OOS-04 | MCP 接入 | 未规划实现 |
| OOS-05 | 向量检索 / RAG | 未规划实现 |
| OOS-06 | 多 agent 调度 | 当前是单会话顺序工作流 |
| OOS-07 | 自动执行模型命令 | 违反 P2 |
| OOS-08 | 交互式 PTY / vim / 交互式密码输入 | 当前是逐次独立 shell |
| OOS-09 | 跨文件事务 | 当前是逐文件原子替换 |
| OOS-10 | 自动 JSON 修复 | 方案 JSON 无效时只报错，不猜 |
| OOS-11 | 真实模型的推理质量 / 计费验收 | 需要用户自己的有效 API 账户 |
| OOS-12 | 用户自定义主题 / 第三方主题包上传 / 在线主题市场 | 本期只做**仓库内目录化**扩展（FR-56）；不提供运行时新增主题的能力，不做主题导入导出，不做主题编辑器 |

> **v1.1 范围澄清（主题改造）**：FR-49 ~ FR-57 属于第 3.1 节「浏览器工作台界面」范围内的 UI 能力增强，**不突破任何既有非目标**——不引入插件运行时（OOS-03）、不新增后端 API、不引入第三方构建链（NFR-13）、不改变单用户本地定位（OOS-01）。唯一需要显式补登记的是 **OOS-12**，用于封住「主题目录化 ⇒ 可运行时扩展主题」的误读：目录化的目的是**降低源码层新增配色方案的成本**，不是给最终用户开放主题定制入口。

---

## 4. 功能需求

### 4.1 会话与历史

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| FR-01 | 新建 / 列表 / 加载会话 | `POST /api/sessions`、`GET /api/sessions`、`GET /api/sessions/{id}` 正常；列表按创建时间倒序 | 已实现·已验证 |
| FR-02 | 会话持久化 | 消息与任务写入 `/data/session-<id>.json`，采用临时文件 + fsync + rename；重启后仍可读 | 已实现·已验证 |
| FR-03 | 会话标题自动生成 | 首条消息取前 32 个字符作为标题 | 已实现·已验证 |
| FR-04 | 历史回放预算控制 | 从最近消息向前收集，文本总量 < 60000 字节；不因超限删除磁盘历史 | 已实现·已验证 |
| FR-05 | 重启后中断标记 | 加载时把 `running` 任务改标为 `interrupted` 并附提示 | 已实现·已验证 |

### 4.2 文件

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| FR-06 | 双根目录浏览 | `root=workspace` 与 `root=context` 均可列目录；目录优先、名称升序 | 已实现·已验证 |
| FR-07 | 文本读取 | `GET /api/file` 返回内容与 sha256 哈希；非 UTF-8 / 含 NUL 拒绝 | 已实现·已验证 |
| FR-08 | 文本写入 | `PUT /api/file` 带 `hash` 做版本校验；空 hash 仅允许新文件 | 已实现·已验证 |
| FR-09 | 原子写入 | 临时文件 + fsync + `rename`，保留原文件权限位 | 已实现·已验证 |
| FR-10 | 路径约束 | 拒绝 `..`、`\`、绝对路径，以及 `.git` / `.data` / `.env` / `docker-images` | 已实现·已验证 |
| FR-11 | 主机实时同步 | 浏览器保存后，宿主机读到相同内容 | 已实现·已验证 |
| FR-12 | 新建文件 | 通过相对路径新建并打开编辑器 | 已实现·已验证 |
| FR-13 | 目录分页 | 单次最多读 2000 项，**当前无翻页接口** | 未实现（见 LIM-09） |

### 4.3 命令面板

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| FR-14 | 单次 bash 执行 | `POST /api/command` 返回 NDJSON 流，含 stdout/stderr 与退出码 | 已实现·已验证 |
| FR-15 | 超时与取消 | 命令最长 60 秒；取消时 `SIGKILL` 整个进程组 | 已实现·已验证 |
| FR-16 | 输出上限 | 超过 128 KiB 截断并提示 | 已实现·已验证 |
| FR-17 | 工作目录边界 | `cwd` 必须落在 `/workspace` 内，用 `EvalSymlinks` 防越界 | 已实现·已验证 |
| FR-18 | 环境隔离 | 子进程只用精简 `PATH`/`HOME`/`GOCACHE`/`GOPATH`，**不继承模型密钥** | 已实现·已验证 |
| FR-19 | 交互式终端 | PTY / WebSocket 生命周期、会话隔离 | 未实现（OOS-08） |

### 4.4 模型接入

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| FR-20 | Chat Completions 兼容适配 | 请求 `{baseURL}/chat/completions`，`stream=false`，取 `choices[0].message.content` | 已实现·已验证（模拟） |
| FR-21 | 模型配置保存与校验 | Base URL 必须是 http/https 且无 userinfo/query/fragment；模型名必填 | 已实现·已验证 |
| FR-22 | 密钥不回传前端 | `GET /api/config` 只返回 `hasKey`；配置文件权限 0600 | 已实现·已验证 |
| FR-23 | 真实模型端到端验收 | 真实账户下：短对话、三阶段 JSON 提案、应用、人工验证各成功一次 | 已实现·已验证（2026-09-22，deepseek-v4-pro：短对话、plan/propose/review、应用、主机侧读回各一次；propose 步骤强制 `response_format=json_object` 防非 JSON 输出） |
| FR-24 | 流式输出（SSE） | Provider 增量响应与前端消费一致，取消/重连/持久化都有测试 | 未实现 |
| FR-67 | 多模型管理 | 模型设置支持多模型列表（id＋名称＋上下文窗口），可增删、标记当前；旧 `settings.json` 单模型格式自动迁移；模型数 ≤20 | 已实现·已验证 |
| FR-68 | 模型名称自动获取 | `GET /api/models` 代理 `{baseURL}/models` 拉取可用模型列表（转发密钥，30s 超时）；前端「自动获取」填入候选列表；不支持/失败时友好提示 | 已实现·已验证 |

### 4.5 AI 工作流

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| FR-25 | 对话模式 | `mode=chat`，单步回答并明确未验证内容 | 已实现·已验证（模拟） |
| FR-26 | 三阶段工作流 | `plan` → `propose` → `review` 三步，逐步持久化与展示 | 已实现·已验证（模拟） |
| FR-27 | 提案结构校验 | 只接受 JSON；最多 10 文件、20 命令、总内容 ≤512 KiB；去重与路径校验 | 已实现·已验证 |
| FR-28 | 附件快照覆盖 | 模型给的 `baseHash`/`before`/`applied` 一律用真实快照覆盖 | 已实现·已验证 |
| FR-29 | 未附加文件不可覆盖 | 已存在但未附加的文件，提案会被拒绝并给明确提示 | 已实现·已验证 |
| FR-30 | 人工应用 | 仅 `awaiting_approval` 状态可应用；应用前重新校验哈希，冲突返 409 | 已实现·已验证 |
| FR-31 | 逐文件原子替换 | 每写成功一个文件就持久化 `applied` 标记，部分失败可识别 | 已实现·已验证 |
| FR-32 | 审查结论自动化 | 解析 review 步骤结论，形成放行/阻断判据 | 未实现（当前"审查完成"只代表模型返回了文字） |
| FR-33 | 工具闭环 | 工具注册、执行记录、审批、结果回传，再让模型根据结果续跑 | 已实现·已验证（由 FR-81 落地：内置四工具 + 插件协议 v1.1 可执行工具，≤10 轮；P2/P3 保留——OOS-07「不自动执行任意模型命令」仍然成立） |

### 4.6 工作台界面

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| FR-34 | 三栏桌面布局 | 左会话、中对话、右文件；窄屏时文件面板转为抽屉 | 已实现·已验证 |
| FR-35 | 任务状态轮询 | 前端 1.2 秒轮询任务状态，展示各步骤进度 | 已实现·已验证 |
| FR-36 | 文件提案 diff 展示 | 可展开查看修改前后内容 | 已实现·已验证 |
| FR-37 | 建议命令一键填入 | 点击填入命令面板**不自动执行**，需再次点击运行 | 已实现·已验证 |
| FR-38 | 附件选择与移除 | 右侧选文件后"附加到任务"，发送时提交 | 已实现·已验证 |
| FR-39 | 窄屏与 1280×720 适配 | 无横向溢出；首页卡片不被裁切 | 已实现·已验证 |
| FR-40 | 富编辑器 | Monaco 级别编辑器、精确 diff 交互 | 未实现 |
| FR-41 | 会话与项目管理增强 | 项目选择、会话重命名/归档、模型配置检测 | 未实现 |
| FR-58 | 品牌设置入口 | 点击左上角 brand 图标打开设置面板；键盘可达；Esc／点击遮罩／关闭按钮均可关闭并归还焦点；窄屏（侧栏隐藏）下顶栏提供等效入口 | 已实现·已验证 |
| FR-59 | 设置 JSON 管理 | 界面设置以单一 JSON 文档持久化于 localStorage 键 `aide.ui`（含 version，非法值回退默认）；旧键 `aide.theme` 自动迁移；设置面板由 `settings-schema.json` 数据驱动渲染，新增设置只需增加 schema 条目；主题收纳为面板「外观」分组首个控件，移除顶栏独立主题按钮 | 已实现·已验证 |
| FR-60 | 玻璃质感滑动 UI | 设置面板为毛玻璃圆角卡片（backdrop blur＋半透明背景＋高光描边），弹性缓动滑入／滑出；主题三态为分段控件，带滑动指示块动画；切换主题 ≤100 ms、无网络请求、不重载页面 | 已实现·已验证 |
| FR-61 | 模型参数配置 | 设置面板新增「模型参数」分组：temperature、top_p、max_tokens、frequency_penalty、presence_penalty、response_format、stop 均可配置；参数随模型调用生效 | 已实现·已验证 |
| FR-62 | 配置 Profile 管理 | 3 个内置系统配置（default／precise／creative）**不可修改、不可删除**；用户配置可 ＋ 添加、－ 删除、自定义命名；全部配置以 JSON 持久化于**工程目录** `profiles.json`（运行时保存写回主机）；非法参数被拒绝 | 已实现·已验证 |
| FR-69 | 侧栏模型选择 | 左侧新增模型选择入口（参考 DSH 模型选择器）：展示当前模型，点击弹出模型列表切换，立即生效于后续任务；任务记录本次所用模型 | 已实现·已验证 |
| FR-71 | 深度研究思考入口 | 新建任务首页 starter 卡片新增「深度研究思考」：点击填入深度研究提示词（chat 模式），覆盖背景梳理、多角度权衡与可验证结论 | 已实现·已验证 |
| FR-87 | 策略弹层整合模型选择 | 策略按钮弹层两栏：策略（auto/手动 profile）+ 模型（单选切换 + 管理入口）；移除侧栏独立模型选择器；按钮标签显示 策略·配置·模型 | 已实现·已验证 |
| FR-91 | 会话轨迹视图 | 顶栏「◉ 轨迹」打开 DSH TrajectoryView 风格时间线：按任务分组展示提示/主题/模型/策略/步骤/工具调用/提案/命令/错误，每任务记录真实 Token 用量（上游缺失标注估算） | 已实现·已验证 |
| FR-92 | 全局聊天搜索 | 顶栏搜索框（⌘K 聚焦）防抖检索全部会话缓存：标题加权 + 正文/摘要片段，点击结果直达会话 | 已实现·已验证 |
| FR-93 | 会话缓存压缩 | 参考 DSH compaction 与 Codex 结构化摘要：历史超 48KB 自动压缩、轨迹面板手动「压缩历史」；折叠消息生成结构化摘要（目标/决策/文件/事实/待办）进入后续上下文，摘要可被搜索命中 | 已实现·已验证 |
| FR-90 | Token 消耗统计 | 设置面板「消耗统计」分组以 git 提交热力图样式展示最近 16 周每日 Token 消耗（强度分级 + 悬停明细 + 今日/累计/调用次数）；每次模型调用记录 usage（上游缺失时按 4 字符/词估算并标记）；统计持久化于 `/data/token-stats.json` | 已实现·已验证 |
| FR-89 | 文件打开 Markdown 渲染与新标签页视图 | 采用公共方案 marked v12（MIT，本地 vendor）实现 GFM 全特性渲染（表格含行内代码/围栏、任务列表、嵌套列表等）+ DOM 消毒 + JS/TS 高亮后处理 | 已实现·已验证 |
| FR-88 | 任务主题自动总结 | 每次新任务先以轻量调用（max_tokens ≤64）总结当前主题（≤32 字）并更新会话标题；失败保留原标题、不阻断任务 | 已实现·已验证 |
| FR-86 | AI 回复 Markdown 渲染与上下文卡压缩 | AI 回复（chat 与 plan/review 步骤）渲染 Markdown（标题/列表/引用/链接/代码块），JavaScript/TS 代码块语法高亮；零依赖原生实现、全量转义防注入；侧栏上下文统计卡压缩为单行 + 细进度条 | 已实现·已验证 |
| FR-72 | 插件面板开关 | 顶栏「文件」按钮左侧新增「插件」按钮；点击后右侧文件面板切换为插件面板，再点文件按钮恢复 | 已实现·已验证 |
| FR-70 | 上下文统计 | 左侧新增上下文统计卡（参考 DSH token-meter）：按 4 字符/词 + 每条消息 4 token 开销估算已用 tokens，进度条对照当前模型的上下文窗口；随会话加载/发送/轮询更新 | 已实现·已验证 |
| FR-63 | 聊天栏策略按钮 | 聊天输入栏左侧小按钮选择策略：`auto`（自动路由）或手动指定某一个 profile；默认手动选择 `default` 配置；任务记录展示本次实际使用的配置 | 已实现·已验证 |
| FR-64 | auto 路由策略 | auto 模式下按工程目录的策略文件（`routing-policy.json` 优先，`routing-policy.md` 内 ```json 块兜底）规则路由到对应 profile：支持按 mode 与 prompt 关键词匹配；无规则命中／文件缺失时回落 `default` | 已实现·已验证 |

### 4.7 部署与运维

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| FR-42 | 一键启动 | 双击 `start.command` 或 `bash scripts/aide.sh start`，自动拉起 Docker、构建、健康检查并带令牌打开浏览器 | 已实现·已验证 |
| FR-43 | 健康检查 | `GET /healthz` 返回 ok；容器 `HEALTHCHECK` 周期 15s | 已实现·已验证 |
| FR-44 | 脚本集 | `start` / `stop` / `status` / `logs` / `test` / `export` / `load` | 已实现·已验证 |
| FR-45 | 镜像导出与导入 | 导出 tar.gz + sha256；`load --no-build` 可还原运行 | 已实现·已验证 |
| FR-46 | 持久化验收脚本 | `verify_runtime.py` 与 `--check` 验证重启后令牌/会话/文件仍在 | 已实现·已验证（会写测试数据，非只读） |
| FR-47 | 异机恢复演练 | 在另一台机器用新项目名/新端口/新卷恢复并验收 | **未实现·未验收（P0）** |
| FR-48 | 远程仓库与协作 | 用户指定 remote 后推送 | 未配置 remote |
| FR-65 | 版本号与 Release Note 管理 | 四位版本号（产品级·重大·大版本·日常）+ RC 补丁号，呈现格式 `X.Y.Z.W RCn`（RC 默认 RC1，同版本补丁 +1）；工程目录 `version.md` 承载当前版本与 release note；`scripts/version.sh` 托管升级（bump 四档 / patch / note / tag / check / install-hooks）；**版本升级以 git tag `vX.Y.Z.W-RCn` 打在对应提交上，且仅 main 分支打 tag**（非 main 分支拒绝执行 bump/patch/note/tag）；git 钩子自动为每次提交（含功能分支）标注当前版本号 | 已实现·已验证 |
| FR-66 | 版本展示 | `/api/config` 返回当前版本；侧栏运行卡片与设置面板底部显示；version.md 缺失时显示 dev | 已实现·已验证 |

### 4.8 插件系统

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| FR-73 | 插件生命周期 | 上传（JS 代码 + 元数据）→ 语法与 DSH 形态校验 → 存入工程目录 `plugins/`；列表、搜索、启用/停用、删除；协议见 `doc/plugin-protocol.md` | 已实现·已验证 |
| FR-74 | 插件协议标准输出 | `doc/plugin-protocol.md`（v1：DSH/Cordis 兼容子集，ctx 支持表、校验规则、surface 格式、安全模型、演进规则）；Node 宿主随二进制 embed | 已实现·已验证 |
### 4.9 工作空间与连接

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| FR-76 | 工作空间配置面板 | 点击侧栏工作空间卡片弹出 Glass 配置面板：工作空间/系统文档/缓存三组，各组支持自定义输入、路径选择与最近使用 3 个路径一键挂载；保存到工程目录 `workspace-config.json` | 已实现·已验证 |
| FR-77 | 本地与 SSH/SFTP 连接 | 工作空间支持本地或 SSH/SFTP 远程映射；host/端口/用户名/密码/私钥均可选（密码与密钥存 `/data` 卷 0600，不落工程目录）；远程模式下命令面板走 SSH 命令输入，**仅允许一个 SSH 会话**（ControlMaster 复用） | 已实现·已验证 |
| FR-78 | 远程文件与 AI 读取 | 远程模式下文件面板浏览/读取/编辑保存、附加与工作流提案应用经 SFTP 完成；AI 附件读取走同一通道 | 已实现·已验证 |
| FR-79 | 本地路径 AI 读取修复 | 配置自定义本地工作空间路径后，文件面板与 AI 附件/提案读取按配置根生效（根目录动态切换）；回归测试覆盖「配置路径 → 附加 → 工作流读到内容」 | 已实现·已验证 |
| FR-82 | 辅助资料来源注册表 | 辅助资料可添加多个来源并自由命名、启停、删除；登记记录存于缓存文件夹 `sources.json`；自动系统文档自动挂载为内置来源并**标记读写**（不可删除） | 已实现·已验证 |
| FR-83 | 多类型来源接入 | 来源类型：local/skill（本地目录，宿主机路径语义）、sftp（复用 SSH 通道）、ftp/ftps/smb/link（curl 通道，只读）、MCP（登记与校验；协议接入标未实现） | 已实现·已验证 |
| FR-84 | 来源浏览与附件 | 文件面板辅助资料按来源浏览/读取/附加；AI 附件经来源通道读取（参考资料语义）；读写标记的本地来源支持编辑器保存 | 已实现·已验证 |
| FR-85 | 预留 | 自动系统文档自动生成机制（本期仅挂载与读写标记） | 未实现 |

| FR-81 | 模型工具闭环 | 内置工具 `list_files`/`read_file` 直接执行、`write_file`/`run_shell` 生成待批准提案；插件协议 v1.1 可执行工具（handler + api）经 Node 宿主调用并回传模型（≤6 轮）；未附加文件不可写（P3）、提案经用户批准才落盘（P2） | 已实现·已验证 |

### 4.10 预留

| FR-80 | 文档与缓存配置生效 | 系统文档路径切换参考根（context 附件从此读取）；缓存路径注入命令环境（GOCACHE/AIDE_CACHE）并自动创建 | 已实现·已验证 |

### 4.10 预留

| FR-75 | 默认预装 DSH 插件集 | 随仓库默认预装 8 个 DSH 能力预设插件（skill/goal/plan/todo/feedback/subagent/terminal/workflow，v1 形态兼容，默认启用，surface 可展示） | 已实现·已验证 |

### 4.8 主题与配色（v1.1 新增）

> 用户需求原文：「页面配色要支持明亮的颜色，配色可选，具备三个按钮（明、暗、跟随系统），UI 做得美观一些，配色方案单独独立成两个目录，方便后期扩展。」
> 已定决策（不再讨论替代方案）：① 配色方案独立成 `light/` 与 `dark/` 两个平行目录；② 沿用绿色系、明暗同源——保留现有深绿 + 薄荷绿气质，明色主题做成浅绿白底。

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| FR-49 | **颜色令牌化**（本次核心工作量） | `style.css` 中不再出现颜色字面量：对 `internal/server/web/style.css` 与 `index.html` 执行 `grep -Eo '#[0-9a-fA-F]{3,8}'`，命中 0 次（改造前为 93 处、89 个不同色值，仅 8 个 CSS 变量）；颜色字面量只允许出现在 `web/themes/**` 的令牌文件内。令牌集合须覆盖：背景层级（页面/面板/次级面板/hover）、边框（常规/强调）、文字（正文/次要/弱化/反色）、主色（brand/accent/on-accent）、语义状态（danger/warn/success）、焦点环、遮罩与阴影。每个令牌在 light 与 dark 两套方案中都必须有定义，缺一即失败 | 未实现 |
| FR-50 | **明色主题** | 存在 `light` 配色方案：浅绿白底（浅色底，不强制纯白）、深色文字；`color-scheme: light`；浏览器原生控件（滚动条、`<dialog>` 默认底色、input 自动填充、文本选中）呈现浅色。命中后 `html` 上的主题属性为 `light`（属性名以架构设计为准），应用内所有区域（侧栏/顶栏/对话区/文件面板/命令面板/模型设置/登录/编辑器/新建文件/toast）无深色残留 | 未实现 |
| FR-51 | **暗色主题** | 存在 `dark` 配色方案，`color-scheme: dark`，保留现有深绿 + 薄荷绿气质；命中后所有界面元素视觉与改造前等效（回归标准见 NFR-16）。暗色是首次打开且系统为暗色时的默认呈现 | 未实现 |
| FR-52 | **三态主题切换控件** | 界面上存在一个三按钮分段控件，按钮文本为 **明 / 暗 / 跟随系统**，同一时刻仅一个按钮处于选中态（语义化选中状态：`aria-checked="true"` 或 `aria-pressed="true"`）。点击任一按钮后 ≤100 ms 页面视觉切换（NFR-17）；控件在 1280×720 与 375 px 宽视口下均完整可见、可点击、无溢出裁切；键盘可达（Tab 聚焦 + 方向键或 Enter 切换）；控件与既有「对话 / AI 工作流」模式切换控件在视觉上可区分，不与模型设置入口混淆 | 未实现 |
| FR-53 | **主题偏好持久化** | 主题偏好写入浏览器 `localStorage`，键名 `aide.theme`，取值限定 `light`/`dark`/`system`（LIM-15、LIM-16）；浏览器刷新后主题与刷新前一致；关闭标签页/浏览器后重开一致；`localStorage` 中该键被人工写入非法值（如 `blue`、空串）时，页面按 `system` 渲染且不报错。主题偏好**不写入服务端、不写入容器卷** | 未实现 |
| FR-54 | **跟随系统实时响应** | 选中「跟随系统」后，改变操作系统外观设置（macOS「系统设置 → 外观」切换浅色/深色，或 DevTools 中模拟 `prefers-color-scheme`）→ 页面在 **1 秒内**自动跟随，无需刷新页面、无需重启容器。用户在「跟随系统」状态下选择「明」或「暗」后，系统外观再变化时页面**不再**跟随，且偏好被持久化为 `light`/`dark` | 未实现 |
| FR-55 | **首屏无主题闪烁** | 在 `localStorage['aide.theme']='light'` 且操作系统为深色的前提下做冷启动（硬刷新 + 禁用缓存）：**首次绘制即为 light**，不得出现「先暗后亮」跳变；页面任何阶段不得出现无主题样式（无令牌）渲染。CSP 为 `script-src 'self'`（NFR-02）且**禁止内联脚本**（NFR-15），因此初始化必须由**同源外链脚本**完成，且执行顺序早于首次内容绘制。验收：录屏逐帧检查无背景色跳变 + Console 无 CSP 违规报错 + 首帧截图与最终态截图主题一致 | 未实现 |
| FR-56 | **配色方案目录化与扩展约定** | 每个配色方案 = `internal/server/web/themes/<theme>/` 下一个独立目录，`light/` 与 `dark/` 为**平行**目录，目录名与枚举取值（LIM-15、LIM-17）一一对应；目录内只允许 CSS 令牌（不得含 JS、不得含图片等二进制资源）；禁止跨主题 `@import`（主题之间不得互相依赖）；新增第 3 个配色方案时，只允许「新增一个目录 + 增加一个枚举取值 + 增加一个切换按钮」，**不得修改既有令牌名，不得改动业务 JS 逻辑**（对话、文件、命令、工作流相关代码零改动） | 未实现 |
| FR-57 | **UI 视觉质量提升**（把"美观一些"拆成可判定项） | ① 所有可交互元素具备 hover / focus-visible / active / disabled 四态样式，且**在明暗两套主题下都可区分**；② 逐组件取样明色主题下前景/背景色对并计算对比度，`.brand`、`.section-label`、`.sessions` 列表项、`.breadcrumb`、`.connection`、对话气泡正文、`.attachment-chips`、`.composer` 提示文字、`.terminal-output`、`.root-tabs`、`#file-path`、`.file-list` 项、`.context-guide`、三个 `<dialog>` 的正文与 `placeholder` 共 ≥14 个取样点全部满足 NFR-12 阈值，结果写入 `docs/verification.md`；③ 不存在任一元素在某一主题下前景与背景同色（不可读）；④ 明色主题下滚动条、`<dialog>` 遮罩、输入框 placeholder、代码/等宽文本区可读；⑤ 1280×720 与 375 px 宽下无横向滚动（延续 FR-39）；⑥ 明暗切换过程中无布局跳动（元素位置不发生非预期偏移） | 未实现 |

---

## 5. 非功能需求

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| NFR-01 | 安全边界 | API 需 `Authorization: Bearer <token>`；`Origin` 与 Host 不一致返 403；令牌用常量时间比较 | 已实现·已验证 |
| NFR-02 | 安全响应头 | `nosniff`、`no-referrer`、严格 CSP、`no-store` | 已实现·已验证 |
| NFR-03 | 最小权限容器 | `cap_drop: ALL`、`no-new-privileges`、非 root（uid/gid 1000）、无 Docker socket 挂载 | 已实现·已验证 |
| NFR-04 | 资源限制 | 2 CPU、2 GiB 内存、256 PID | 已实现·已验证 |
| NFR-05 | 只读参考资料 | `/context` 在 Docker 层 `RW=false`，写入探针返回 Read-only | 已实现·已验证 |
| NFR-06 | 目录逃逸防护 | 文件 API 基于 `os.Root`；命令 `cwd` 二次校验 | 已实现·已验证 |
| NFR-07 | 代码质量门禁 | `go test -race -count=1 ./...` + `go vet ./...` 通过；Dockerfile 构建阶段同样先跑测试 | 已实现·已验证 |
| NFR-08 | 前端无构建依赖 | 原生 JS/CSS，`go:embed` 进二进制，无 CDN 依赖、无 npm install | 已实现·已验证 |
| NFR-09 | 数据原子性 | 会话与设置写入均走临时文件 + fsync + rename | 已实现·已验证 |
| NFR-10 | 备份完备性 | 源码 + 镜像 + 工作区文件 + 两个 Docker 卷，四类资产可独立备份还原 | 已实现·未验收（缺异机演练） |
| NFR-11 | 会话格式版本迁移 | 数据结构变更时的迁移机制 | 未实现 |
| NFR-12 | **明色主题对比度下限** | **适用范围（v1.2 依据用户裁决界定）**：本条**只判定 `light` 明色主题**。这是**适用范围界定，不是豁免**——NFR-12 自 v1.1 立项起即名为「明色主题对比度下限」，从来没有要求改变暗色主题；暗色主题的外观由 NFR-16「保真优先」判定，其**既有**边界对比度不纳入本条（既有 3 处低于 3:1 的边界已登记为 R-13，本次不修）。<br>**判定口径（仅明色）**：正文文字与其背景对比度 ≥ **4.5:1**；大号字（≥24 px，或 ≥18.66 px 加粗）、UI 组件边界、图标、焦点环、语义状态色 ≥ **3:1**。测量方式：按 WCAG 2.1 相对亮度公式对取样色值计算，结果记入 `docs/verification.md`。<br>**实现注意**：现有 `--green:#b4efcc`、`--accent:#83d9a8` 属高明度薄荷绿，直接用于浅底会不达标，明色主题必须另取深绿强调色 | 未实现 |
| NFR-13 | **零第三方依赖、无构建步骤** | `go.mod` 保持 `module aide` + `go 1.26` 两行不变，不新增任何 Go 依赖；不引入 Tailwind / PostCSS / Sass / Less / 任何 npm 包或构建产物；仓库内不得新增 `package.json` / `node_modules` / lockfile / 构建脚本；主题与令牌必须用纯 CSS + 原生 JS 实现，不得要求 `npm install`。验收：`git diff --stat` 不含 `go.mod`、不含 `go.sum`；仓库根无 `package.json`；`docker compose up -d --build` 不需要任何前置安装步骤即可成功 | 未实现 |
| NFR-14 | **主题资源随二进制嵌入** | 新增主题目录与主题初始化脚本必须随 `go:embed` 编译进二进制，运行时不得依赖宿主机文件；容器内不得出现对 `web/` 的运行时读取。**待架构师核实**：`server.go` 第 27 行当前为 `//go:embed web/*`，其是否递归包含新增子目录 `web/themes/light/`、`web/themes/dark/`（见增量 PRD 待确认问题 Q1）。验收：在**不带** `web/` bind mount 的运行实例中通过浏览器 Network 面板确认主题令牌资源返回 200 且内容与仓库一致；不得以「宿主机有文件所以能看到」作为通过依据 | 未实现 |
| NFR-15 | **CSP 约束下不得使用内联脚本** | `server.go` 的 CSP 保持 `default-src 'self'; script-src 'self'; style-src 'self'; connect-src 'self'; img-src 'self' data:; frame-ancestors 'none'; base-uri 'none'`，**不得**为绕过限制而放宽为 `'unsafe-inline'`、增加 nonce 或 `unsafe-hashes`。页面不得出现内联 `<script>`、内联事件处理器（`onclick=` 等）、`javascript:` URL、内联 `<style>`。验收：CSP 响应头与 NFR-02 记录逐字符一致；浏览器 Console 无 CSP 违规；`index.html` 内无内联脚本块 | 未实现 |
| NFR-16 | **暗色主题视觉回归不退化** | 以基线 `fbe45b2` 的 `style.css` 为参照，在 1280×720、暗色主题下对关键界面（首页欢迎区、会话区、输入区、命令面板、文件面板、三个弹窗）逐区域截图比对：允许的差异仅限「等价令牌替换」导致的通道偏差 ≤ 2/255，不得出现元素错位、边框缺失、字号变化、可读性下降。`docs/verification.md` 已记录的布局指标不得反转：`.welcome` 卡片底部 406.86 px 仍小于 `.conversation` 底部 484.30 px；页面仍无横向溢出 | 未实现 |
| NFR-17 | **主题切换时延与无副作用** | 主题切换（含首屏应用与运行时切换）为纯前端行为：不重载页面、不发任何网络请求、不重建 Docker 镜像、不中断正在进行的任务状态轮询（FR-35）与命令面板输出流（FR-14）。切换视觉完成 ≤ 100 ms（与 FR-52 一致）；「跟随系统」的响应 ≤ 1 s（与 FR-54 一致） | 未实现 |

---

## 6. 技术现状基线

> 本节描述"现在是什么样"，不是"应该是什么样"。改动实现方式后必须同步本节。

### 6.1 技术栈

| 层 | 选型 |
| --- | --- |
| 后端 | Go 1.26（`module aide`），标准库 `net/http`，无第三方依赖 |
| 前端 | 原生 JavaScript + CSS，`go:embed` 嵌入二进制；**主题能力（v1.1 目标态）**：纯 CSS 自定义属性（颜色令牌）+ 原生 JS，按方案分目录组织，无构建步骤、无第三方依赖（NFR-13） |
| 容器 | Docker + Compose；镜像 `aide:local`，`linux/arm64` |
| 容器内工具链 | Go 1.26.8、Python 3.12.14（含 venv/pip）、Node.js 24.21.0 + npm 11.19.0、Git 2.39.5、curl、bash |
| 平台 | Apple Silicon Mac + Docker Desktop |

### 6.2 模块边界

| 文件 | 行数 | 责任 |
| --- | --- | --- |
| `cmd/aide/main.go` | 12 | 应用入口，`server.Run()` 出错即退出 |
| `internal/server/server.go` | 342 | 鉴权中间件、路由、配置、会话持久化、Profile 加载、优雅关闭 |
| `internal/server/provider.go` | 88 | 单次 Chat Completions 请求、Profile 参数透传、reasoner 参数剔除、超时、错误处理 |
| `internal/server/workflow.go` | 364 | 任务状态机、三个模型步骤、策略解析、提案校验、文件应用 |
| `internal/server/files.go` | 200 | `os.Root` 沙箱、大小限制、哈希校验、原子写入 |
| `internal/server/command.go` | 134 | 进程组管理、精简环境、NDJSON 流、超时终止 |
| `internal/server/server_test.go` | 278 | 9 个测试函数 |
| `internal/server/web/` | — | `index.html` / `app.js` / `style.css`（界面骨架、逻辑与令牌化后的样式）；另有 1 个**同源外链**主题初始化脚本用于首屏防闪（FR-55，文件名待架构设计确定） |
| `internal/server/web/themes/` | 本次新增 | 配色方案根目录：**每个方案一个平行子目录**，目录内只放 CSS 令牌（FR-56） |
| `internal/server/web/themes/light/` | 本次新增 | 明色方案令牌（浅绿白底、深色文字、深绿强调色） |
| `internal/server/web/themes/dark/` | 本次新增 | 暗色方案令牌（深绿 + 薄荷绿，与改造前视觉等效） |

### 6.3 任务状态机

```text
running → completed                        对话，或无文件提案
running → awaiting_approval → completed     有文件提案，经用户应用
running → failed / cancelled               模型错误、协议错误、停止、超时
running → interrupted                      服务重启后的恢复标记
```

### 6.4 API 契约

除静态资源与 `/healthz` 外，全部需要 `Authorization: Bearer <token>`。

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/healthz` | 健康检查（免鉴权） |
| GET | `/api/config` | 不含密钥的配置与运行信息 |
| PUT | `/api/settings` | 保存 API 地址、模型、密钥（支持 `clearKey`） |
| GET | `/api/files?root=workspace&path=.` | 列目录 |
| GET | `/api/file?root=workspace&path=README.md` | 读文本与版本哈希 |
| PUT | `/api/file` | `{path,content,hash}` |
| GET / POST | `/api/sessions` | 列出 / 创建会话 |
| GET | `/api/sessions/{id}` | 完整会话与任务状态 |
| POST | `/api/sessions/{id}/runs` | `{prompt,mode,attachments}` |
| POST | `/api/sessions/{id}/runs/{run}/cancel` | 取消任务 |
| POST | `/api/sessions/{id}/runs/{run}/apply` | 应用已审查的文件方案 |
| POST | `/api/command` | `{command,cwd}`，返回 NDJSON |

附件格式：`[{"root":"workspace","path":"README.md"}]`。只有 workspace 可写。

> **v1.1 主题改造的 API 影响：无。** 主题为纯前端能力，不新增、不修改任何端点的契约；不新增服务端配置项（主题偏好只存浏览器 `localStorage`，见 LIM-16，不落 `/data/settings.json`）；主题令牌资源属静态资源，沿用现有静态资源规则（免鉴权）。因此本节路由表与鉴权要求**不变**。

### 6.5 量化限制（改动必须同步此处）

| 编号 | 对象 | 限制 | 定义位置 |
| --- | --- | --- | --- |
| LIM-01 | HTTP JSON 请求体 | 1 MiB | `server.go: decode` |
| LIM-02 | 用户任务长度 | 1 – 20,000 字节 | `workflow.go: startTask` |
| LIM-03 | 显式附件 | 最多 8 个；拼装后 ≤80,000 字节 | `startTask` |
| LIM-04 | 单个文本文件 | 256 KiB；拒绝非 UTF-8 / 含 NUL | `files.go: readText` |
| LIM-05 | 文件提案 | ≤10 文件；总内容 ≤512 KiB；≤20 条命令 | `acceptProposal` |
| LIM-06 | 历史回放 | 文本总量 <60,000 字节 | `startTask` |
| LIM-07 | 模型并发 | 每会话 1 个；全服务 4 个 | `startTask` |
| LIM-08 | 模型时限 | 单次请求 120 秒；整体任务 6 分钟 | `provider.go` / `workflow.go` |
| LIM-09 | 命令 | 最多 4 并发；最长 60 秒；输出 ≤128 KiB；命令串 ≤16,000 字节 | `command.go` |
| LIM-10 | 目录浏览 | 单次 ≤2,000 项，无翻页 | `files.go: listFiles` |
| LIM-11 | 命令会话 | 每次独立 shell；不保留 `cd`；无 stdin / PTY | `command.go` |
| LIM-12 | HTTP 服务 | 读头超时 10s；空闲超时 60s；最大头 16 KiB | `server.go: Run` |
| LIM-13 | 模型响应体 | 读取上限 2 MiB | `provider.go` |
| LIM-14 | 容器资源 | 2 CPU / 2 GiB / 256 PID | `compose.yaml` |
| LIM-15 | 主题枚举 | 取值仅 `light` / `dark` / `system` 共 3 个；未写入偏好时默认 `system`；非法值或读取失败一律回退 `system`，不得抛错 | 前端脚本（本次新增） |
| LIM-16 | 主题偏好存储键 | `localStorage` 键名固定为 **`aide.theme`**，值为 LIM-15 取值之一；不使用其它键名、不使用 cookie、不写入服务端与容器卷。**命名注意**：现有访问令牌键为 `aide-token`（连字符），本键用点号，属历史不一致，本期不统一（见增量 PRD Q4） | 前端脚本（本次新增） |
| LIM-17 | 配色方案数量 | 本期落地 2 个方案（`light`、`dark`），与 `themes/` 下目录一一对应；`system` 不是配色方案、不占目录；架构须支持在 **≤8** 个方案内仅靠「新增目录 + 枚举取值 + 控件按钮」扩展（FR-56） | `web/themes/`（本次新增） |
| LIM-18 | 主题资源体积 | 单个主题令牌文件 ≤ **8 KiB**；`internal/server/web/` 目录总体积 ≤ **200 KiB**（改造前 3 个文件合计约 39 KiB）；单次主题资源请求 ≤ 32 KiB | `web/themes/**` |
| LIM-19 | 主题生效时限 | 首屏主题应用必须早于首次内容绘制（不晚于 `DOMContentLoaded`），硬刷新首帧不得出现非目标主题；运行时切换视觉完成 ≤ 100 ms；「跟随系统」响应 ≤ 1 s | 前端脚本（本次新增） |
| LIM-20 | **主题令牌集合（已冻结）** | 令牌清单冻结，**数量基线 79 个**——即在 Δ≤2/255 保真度下复现改造前 89 个颜色字面量所需的**最少色阶数**（架构师设计结论）。约束：① `light` 与 `dark` 两套方案必须定义**同名、同数量**的全部 79 个令牌，缺一即失败；② **令牌名是稳定接口**，后续新增配色方案不得改名、不得增删令牌；③ 如需变更令牌集合（增 / 删 / 改名 / 调基线），须单独提需求并同步本表与增量 PRD；④ 不得为"看起来更整洁"而精简令牌数量——精简会直接突破 NFR-16 的 ≤2/255 保真约束 | `web/themes/**`（本次新增） |

### 6.6 挂载与存储

| 主机路径（默认） | 容器路径 | 用途 | 备份方式 |
| --- | --- | --- | --- |
| 当前 `aide/` | `/workspace` | 可读写项目 | Git + 文件备份 |
| 上一级 `Harness/` | `/context` | 只读参考资料 | 单独保存或重建 |
| 卷 `aide_aide-data` | `/data` | `access-token`、`settings.json`、`session-*.json` | **必须单独备份**，可能含密钥与私有对话 |
| 卷 `aide_aide-home` | `/home/aide` | 工具缓存与用户配置 | 有自定义环境时迁移 |

**配置优先级**：`/data/settings.json` **整体优先于** `.env` 环境变量。只改 `.env` 可能不生效，应从浏览器模型设置更新。

> 浏览器 `localStorage`（不属于容器卷，**不参与备份**）：`aide-token`（访问令牌，README 已述）、`aide.theme`（主题偏好，LIM-16）。因此主题偏好不随「源码 + 镜像 + 工作区 + 两卷」四类资产迁移，换浏览器/清缓存后回到 `system`，此为可接受行为。

### 6.7 环境变量

```dotenv
AIDE_PORT=8097            # 主机端口，只绑 127.0.0.1
AIDE_WORKSPACE=.          # → /workspace，可读写
AIDE_CONTEXT=../Harness   # → /context，只读
AI_BASE_URL=https://api.deepseek.com
AI_MODEL=
AI_API_KEY=
```

`.env` 已被 Git 忽略；不存在时复制 `.env.example`，**不要覆盖已有 `.env`**。

### 6.8 当前运行实例

| 项 | 值 |
| --- | --- |
| 容器 | `aide-aide-1`，`Up (healthy)` |
| 端口 | `127.0.0.1:8097 -> 8080/tcp` |
| 镜像 ID | `sha256:6736e4bd765a1c653fa85daba29eae8d6b8b0c56f72167f3f9b220043fcfadc3` |
| 镜像归档 | `docker-images/aide-local.tar.gz`，约 334 MiB |
| 归档 SHA-256 | `46d226a838243e6a7627442dee784e54b6971e43d7463f13d881909f72ac0707` |
| 运行身份 | `uid=1000(aide) gid=1000(aide)` |
| 基础镜像 | 三个官方镜像均按 digest 固定 |

---

## 7. 验收状态汇总

| 维度 | 已实现·已验证 | 已实现·未验收 | 未实现 |
| --- | --- | --- | --- |
| 会话 | FR-01 ~ FR-05 | — | — |
| 文件 | FR-06 ~ FR-12 | — | FR-13 |
| 命令 | FR-14 ~ FR-18 | — | FR-19 |
| 模型 | FR-20 ~ FR-22（模拟） | FR-23（**真实模型**） | FR-24 |
| 工作流 | FR-25 ~ FR-31（模拟） | FR-23（真实 JSON 遵循能力） | FR-32、FR-33 |
| 界面 | FR-34 ~ FR-39 | — | FR-40、FR-41 |
| 部署 | FR-42 ~ FR-46、FR-48 部分 | FR-47（**异机恢复**） | FR-48（remote） |
| 主题与配色（v1.1） | — | — | **FR-49 ~ FR-57（全部）** |

**一句话结论**：文件、命令与真实 AI 功能均可用；真实模型端到端已于 2026-09-22 验收（deepseek-v4-pro：短对话、三阶段 JSON 提案、应用、主机侧验证各一次）。剩余未验收项：异机恢复演练（FR-47）、远程仓库（FR-48）。

**v1.1 增补**：主题与配色改造（FR-49 ~ FR-57、NFR-12 ~ NFR-17）**尚未开工**，全部为「未实现」；其验收必须包含「重建镜像后在浏览器实测」与「明色主题对比度实测」两项，不接受仅以代码 review 或本地静态预览作为通过依据。

**v1.2 增补（范围界定）**：NFR-12 的判定范围明确为**仅明色主题**——暗色主题改由 NFR-16 保真判定，其既有 3 处低对比边界（R-13）**不纳入** NFR-12 判定，也不在本次修复范围。因此本次主题改造的验收口径为：**明色主题按 NFR-12 判定达标；暗色主题按 NFR-16 判定不退化**，两者并行、互不替代。

后端 9 个测试函数覆盖：鉴权与 Origin、文件边界与冲突、密钥不返回、工作流审批冲突与持久化、未附加文件拒绝、Provider 错误与取消、命令执行与退出码、重启中断标记、对话历史与取消端点。

---

## 8. 已知缺陷与风险

| 编号 | 问题 | 影响 | 状态 |
| --- | --- | --- | --- |
| R-01 | "审查完成"无自动判据 | 用户仍须逐条审阅提案 | 待 FR-32 |
| R-02 | 应用非跨文件事务 | 部分失败会留下 `applied` 标记，需人工核对 | 待设计事务/备份策略 |
| R-03 | 无会话格式版本迁移 | 改数据结构可能读到旧格式 | 待 NFR-11 |
| R-04 | 附件非持久快照库 | 新一轮分析需重新附加文件 | 待设计 |
| R-05 | 镜像归档仅 ARM64 | x86 环境需另行构建验收 | 见 FR-47 |
| R-06 | 0600 权限不等于加密 | 手动 shell 仍有容器用户对数据卷的权限 | 已接受 |
| R-07 | 目录无翻页 | 大目录只见前 2000 项 | 见 FR-13 |
| R-08 | 命令面板权限高于文件 API | 文件 API 隐藏 `.git`/`.env`，但命令面板可读 | 已接受（可信单用户前提） |
| R-09 | **前端色值硬编码散落**：`style.css` 单行压缩，93 处十六进制、89 个不同色值，仅 8 个 CSS 变量 | 明色主题无法只靠改变量实现；令牌化遗漏一处，该组件在明色主题下就可能不可读（白字白底 / 深字深底） | 待 FR-49、FR-57（本次改造的直接动因） |
| R-10 | `//go:embed web/*` 是否**递归**包含新增子目录 `web/themes/**` 未经验证 | 若不递归，主题令牌不会进二进制，容器内请求 404；本地预览正常但运行实例样式缺失，属「本地看着好、部署就坏」类风险 | 待架构师核实（增量 PRD Q1，不得主观下结论） |
| R-11 | 明色主题对比度未实测，且**绿色系 + 浅底天然不达标**：现有 `--green:#b4efcc`、`--accent:#83d9a8` 亮度高，白底上对比度明显不足 4.5:1 | 若明色主题直接复用现有强调色，会出现「看起来漂亮但读不清」，且违反 NFR-12 | 待 NFR-12 实测；明色方案须另取深绿强调色（FR-50） |
| R-12 | 主题令牌化是大范围 CSS 改写（单一压缩文件、~300 行规则内联） | 一处令牌名写错或漏替代，即造成暗色主题视觉回归；且压缩格式 diff 不可读、难以逐条 review | 待 NFR-16 截图回归比对；建议改造后仍保持可读的格式化输出 |
| R-13 | **暗色主题既有交互边界对比度低于 3:1**（架构师实测，改造前基线 `fbe45b2` 即存在，**非本次变更引入**）：`input` 输入框边界 **1.36:1**、`.quiet` 次要按钮边界 **1.72:1**、`.composer` 输入区外框 **2.58:1** | 暗色主题下输入框与次要按钮的边界不易辨识，弱视觉/低照度环境下更明显；键盘用户依赖 2px 绿色焦点环定位（该环本身达标） | **本次不修**。依据用户裁决：暗色严格原样，受 NFR-16（逐通道偏差 ≤2/255）保真约束，改动这 3 处即判回归。已配套：NFR-12 适用范围限定为明色（判定口径已明确）、本项登记为已知缺陷、后续路线详见第 9 节 P2「暗色主题既有低对比边界修复」 |

---

## 9. 后续路线图

以下为建议优先级，**不代表已获授权开发**。开工前请先在本文登记 FR 编号。

| 优先级 | 工作项 | 最低完成标准 |
| --- | --- | --- |
| P0 | ~~真实 DeepSeek / 目标兼容模型验收~~ | ✅ 已完成 2026-09-22（deepseek-v4-pro，见 FR-23；未保存密钥） |
| P0 | 异机备份恢复演练 | 独立目录与新卷恢复成功；历史会话 / 挂载 / 只读约束 / 登录均验证 |
| P1 | 模型输出与失败体验 | JSON 校验反馈更清楚；区分用户停止与超时；重试受控且可审阅 |
| P1 | 文件应用一致性 | 宕机或写入失败时可准确识别已写文件；设计备份或事务策略并测试恢复 |
| P1 | 工作流工具闭环 | 工具注册、执行记录、审批、结果回传；**不能直接执行任意模型文本** |
| P1 | 流式交互 | Provider 增量响应与前端消费一致；取消、重连、最终持久化都有测试 |
| P2 | 交互终端与编辑器 | PTY / WebSocket 生命周期、会话隔离、退出回收；编辑器与 diff 交互完善 |
| P2 | 项目与会话管理 | 项目选择、会话重命名/归档、目录分页、模型配置检测；保留数据兼容性 |
| P2 | 外部扩展 | 按明确需求规划 MCP、插件接口、检索或多 agent；分别定义权限和验证标准 |
| P2 | 主题可访问性与扩展 | 对比度自动校验脚本（防后续配色方案回归）；`prefers-contrast` / 高对比度模式适配；新增第 3 个配色方案（仅加目录 + 枚举 + 按钮，见 FR-56）；**不含**用户自定义主题与在线主题市场（OOS-12） |
| P2 | **暗色主题既有低对比边界修复**（对应 R-13） | 修正 3 处既有交互边界至 ≥3:1：`input` 输入框边界（当前 1.36:1）、`.quiet` 次要按钮边界（1.72:1）、`.composer` 输入区外框（2.58:1）；**同步放宽 NFR-16 对这几处的约束**（否则修复即被判定为视觉回归）；出具修复前后对比度实测数值并记入 `docs/verification.md`。本项为独立需求，须先登记 FR 编号再开工 |

---

## 10. 开发前需求核对清单

每次开工，逐项打勾或写明例外：

- [ ] `git status --short` 干净，或在开工前已明确交代当前未提交改动
- [ ] `git log -3 --oneline` 基线未被他人的提交推移
- [ ] 本次开发已从 `main` 创建独立功能分支（见 0.4）；验证通过后才允许合并回 `main`
- [ ] 版本号与 release note 已按 `scripts/version.sh` 更新（FR-65 / LIM-24）
- [ ] 本次要做的需求已有 `FR-xx` 编号；没有就先补到第 4 节
- [ ] 该 FR 的当前状态与代码实际情况一致（不轻信文档里写的"已完成"）
- [ ] 本次改动**不在第 3.2 节非目标列表**内；若在，先改需求
- [ ] 方案未突破第 6.5 节量化限制；若突破，需连带更新限制表
- [ ] 方案不违背第 1.3 节六条设计原则
- [ ] 涉及模型/文件/命令/取消逻辑的改动，已计划补对应行为测试
- [ ] 涉及 `web/`（前端）的改动，已计划 `docker compose up -d --build` **重建镜像**后再验证（前端经 `go:embed` 进二进制，不重建则页面不变）
- [ ] 涉及挂载、端口、卷的改动，已确认备份与回退路径
- [ ] 明确本次的验证方式（单测 / 模拟 API / 真实调用 / 主机侧读取），并知道**模拟通过不等于真实通过**
- [ ] 收尾时更新第 7 节状态矩阵与第 11 节变更记录

---

## 11. 变更记录

| 日期 | 版本 | 变更 | 作者 |
| --- | --- | --- | --- |
| 2026-09-21 | v1.0 | 依据 `bb2d1b6` 基线与运行实例核对结果建立本文档 | 编码助手 |
| 2026-09-21 | v1.1 | **主题与配色增量**（用户需求：明亮配色 + 明/暗/跟随系统三按钮 + 配色方案独立两目录 + UI 美化）。新增 4.8 节 FR-49 ~ FR-57（令牌化、明色、暗色、三态切换、持久化、跟随系统、首屏无闪烁、目录化扩展、视觉质量）；新增 NFR-12 ~ NFR-17（对比度下限、零依赖无构建、embed 嵌入、CSP 无内联脚本、暗色回归不退化、切换时延）；新增 LIM-15 ~ LIM-19（枚举、键名 `aide.theme`、方案数量、体积、生效时限）；新增 OOS-12（用户自定义/第三方主题打包）；3.1 补登记主题范围；6.1/6.2 同步目标态目录结构；6.4 明确主题无 API 影响；6.6 补浏览器存储说明；第 7 节新增主题行；第 8 节新增 R-09 ~ R-12；第 9 节新增 P2 路线；第 10 节新增前端改动重建镜像核对项。详见 [doc/prd/2026-09-21-theme-switching.md](prd/2026-09-21-theme-switching.md) | 产品经理 许清楚 |
| 2026-09-21 | v1.2 | **依据用户裁决界定 NFR-12 适用范围**：架构师在设计中发现 NFR-12（明暗均需满足组件边界 ≥3:1）与 NFR-16（暗色保真 ≤2/255）冲突——改造前暗色既有 3 处边界即低于 3:1（`input` 1.36:1、`.quiet` 1.72:1、`.composer` 2.58:1，非本次引入）。用户裁决「暗色严格原样，只做明色」，据此：NFR-12 判定范围明确为**仅 `light`**（属适用范围界定而非豁免），暗色既有边界本次不修；新增 **R-13**（该 3 处既有低对比边界及其不修原因）；新增 **LIM-20**（令牌集合冻结，数量基线 **79**）；第 9 节新增 P2「暗色主题既有低对比边界修复」；第 7 节新增 v1.2 增补说明；文档头同步至 v1.2。同步更新 [doc/prd/2026-09-21-theme-switching.md](prd/2026-09-21-theme-switching.md) 的 A14 / B1 / C8 / Q4 判定口径 | 产品经理 许清楚 |

---

## 附：相关文档

- [HANDOVER.md](../HANDOVER.md) — 接手定位、十分钟上手、备份恢复、排障、镜像导出
- [docs/architecture.md](../docs/architecture.md) — 架构图、模块边界、API 细节
- [docs/verification.md](../docs/verification.md) — 镜像 ID、SHA-256、测试项与浏览器实测记录
- [README.md](../README.md) — 面向使用者的启动与使用说明

---

## 12. 产品化与架构重构记录（2026-09-22）

本轮按用户要求对产品做了整体审视，已完成的重构与新增：

| 项 | 结论 |
| --- | --- |
| 设置面板 | JSON 驱动侧边导航 + 二级子菜单（settings-schema.json 单点扩容）；每次打开强制重渲染 |
| Markdown | 引擎升级 marked v12（vendor、MIT）GFM 全特性 + DOM 消毒 + JS 高亮后处理 |
| 上下文卡 | 压缩为单行 + 细进度条（口径入 tooltip） |
| 策略弹层 | 双栏（左策略右模型）独立滚动，侧栏重复入口已移除 |
| Token 统计 | git 热力图 + 计价可配置 + 单日账单 + 余额查询 |
| 轨迹（新） | DSH 风格事件时间线 + 每任务 Token 用量 |
| 搜索（新） | ⌘K 全局检索会话缓存 |
| 压缩（新） | 自动 + 手动 compaction（结构化摘要） |
| 浏览器缓存 | 静态资源 ?v=N 递增；页面恢复可绕过缓存，诊断用 ui 标记 |

后续产品化路线（按优先级）：流式输出（FR-24）、PTY 终端（FR-19）、目录分页（FR-13）、会话归档/重命名（FR-41）、MCP 真实接入、工具循环插件化（协议 v1.1 已就绪）。

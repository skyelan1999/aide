.env
.data/
.DS_Store
bin/
test-results/
tmp/
*.log
docker-images/*.tar*
docker-images/*.sha256

# 编码助手本地记忆，仅保留在本机，不进入版本库
.workbuddy/

# IDE 本地配置
.idea/
plugins/surface.json
workspace-config.json
.cache/
.agent-state/
__pycache__/

# 演练备份示例（含合成令牌，仅保留在本机验收目录，不入库）
docs/reviews/*/evidence/drill/*.tar.gz

# 本地验证二进制产物
.verify-bin/

# html skill self-check screenshots (non-deliverable)
_shots/

# 插件 node_modules（可 npm install 重建，不入库）
node_modules/
plugins/*/node_modules/

# 运行时配置（含本地密钥/令牌，不入库）
profiles.json
.backup-assistant/

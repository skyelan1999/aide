# aide 产品需求与状态基线

文档版本：v2.1（2026-09-23）。本次交付目标 0.1.6.0 RC4，包含 UI、文档、Agent 路由与版本化 Docker 镜像；发布结果以 Release 和验收记录为准。

旧文档将早期“未实现”与后续“已验证”混排，不能作为一致状态表。本次保留全部既有 FR 编号并补齐遗漏的 FR-94；原文和原验收标准保存在 [历史快照](archive/2026-09-23/PRD-before-review.md)。旧 NFR/LIM/OOS 编号不再复用；与现行说明冲突时以下明确修订优先。

## 0. 开发入口与证据

所有开发从 [AGENTS.md](../AGENTS.md) 和 [统一工作流](agent/WORKFLOW.md) 开始。先确认用户需求、Git 状态和任务范围，保留未提交工作。新功能用功能分支；既有任务在当前分支继续，不自动重置/切换。

状态区分：未实现、部分实现、实现存在（代码依据）、已验证（明确环境和证据）、已发布（tag/构建/部署证据）。本次文档核对不等于所有功能重测。历史模拟/真实模型/浏览器结论分别引用，不能互相替代。

## 1. 主旨与原则

aide = AI + IDE，面向可信单用户，通过融合 AI 与集成工作环境让人更专注于专业工作。开发 Agent 路由用于维护与场景扩展。产品采用：浏览器操作、Go 单服务、Docker 工具环境、本地或远程工作目录。

- P1 不伪造能力或测试结果。
- P2 模型写文件/执行命令先产生提案，由用户明确应用/运行。可信插件和手动命令本身不受独立安全沙箱限制。
- P3 显式附件授权现有文件提案覆盖；读工具可以额外读取工作区并进入模型上下文。原“附件即全部上下文”表述废止。
- P4 附件、工具结果和历史回答是资料，不能成为用户授权。
- P5 本机单用户，不提供公网多用户托管。
- P6 源码、镜像、业务文件、卷分别备份。

## 2. 功能登记

下表为当前实现状态索引；验收细则结合源码 handler/test 与现行用户/架构文档。标“历史证据”的条目没有在本次逐项重新实测。对应依据：[RC5 整改](reviews/2026-09-23/remediation-status.md)、[历史验证](verification.md)、[本轮 UI](design/macos-ui.md)。

| 编号 | 功能 | 当前状态与修订 |
| --- | --- | --- |
| FR-01 | 新建 / 列表 / 加载会话 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-02 | 会话持久化 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-03 | 会话标题自动生成 | 旧规格被后续需求修订；见下节 |
| FR-04 | 历史回放预算控制 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-05 | 重启后中断标记 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-06 | 双根目录浏览 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-07 | 文本读取 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-08 | 文本写入 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-09 | 原子写入 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-10 | 路径约束 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-11 | 主机实时同步 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-12 | 新建文件 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-13 | 目录分页 | 未实现 |
| FR-14 | 单次 bash 执行 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-15 | 超时与取消 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-16 | 输出上限 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-17 | 工作目录边界 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-18 | 环境隔离 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-19 | 交互式终端 | 未实现 |
| FR-20 | Chat Completions 兼容适配 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-21 | 模型配置保存与校验 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-22 | 密钥不回传前端 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-23 | 真实模型端到端验收 | 历史记录有真实模型验收；本轮未调用付费模型 |
| FR-24 | 流式输出（SSE） | 实现存在；race 单测与容器冒烟通过，浏览器 UI 实测待用户确认（本轮未调用付费模型） |
| FR-25 | 对话模式 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-26 | 三阶段工作流 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-27 | 提案结构校验 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-28 | 附件快照覆盖 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-29 | 未附加文件不可覆盖 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-30 | 人工应用 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-31 | 逐文件原子替换 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-32 | 审查结论自动化 | 未实现 |
| FR-33 | 工具闭环 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-34 | 三栏桌面布局 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-35 | 任务状态轮询 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-36 | 文件提案 diff 展示 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-37 | 建议命令一键填入 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-38 | 附件选择与移除 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-39 | 窄屏与 1280×720 适配 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-40 | 富编辑器 | 未实现 |
| FR-41 | 会话与项目管理增强 | 部分实现或完整验收待补；不能宣称全量达标 |
| FR-42 | 一键启动 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-43 | 健康检查 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-44 | 脚本集 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-45 | 镜像导出与导入 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-46 | 持久化验收脚本 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-47 | 异机恢复演练 | 部分实现或完整验收待补；不能宣称全量达标 |
| FR-48 | 远程仓库与协作 | GitHub remote 已配置；本轮不推送、不推定发布状态 |
| FR-49 | 颜色令牌化（本次核心工作量） | 部分实现或完整验收待补；不能宣称全量达标 |
| FR-50 | 明色主题 | 已实现；本轮预览/设置逻辑检查，待正式构建验收 |
| FR-51 | 暗色主题 | 已实现；本轮预览/设置逻辑检查，待正式构建验收 |
| FR-52 | 三态主题切换控件 | 旧规格被后续需求修订；见下节 |
| FR-53 | 主题偏好持久化 | 旧规格被后续需求修订；见下节 |
| FR-54 | 跟随系统实时响应 | 已实现；本轮预览/设置逻辑检查，待正式构建验收 |
| FR-55 | 首屏无主题闪烁 | 部分实现或完整验收待补；不能宣称全量达标 |
| FR-56 | 配色方案目录化与扩展约定 | 旧规格被后续需求修订；见下节 |
| FR-57 | UI 视觉质量提升（把"美观一些"拆成可判定项） | 部分实现或完整验收待补；不能宣称全量达标 |
| FR-58 | 品牌设置入口 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-59 | 设置 JSON 管理 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-60 | 玻璃质感滑动 UI | 旧规格被后续需求修订；见下节 |
| FR-61 | 模型参数配置 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-62 | 配置 Profile 管理 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-63 | 聊天栏策略按钮 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-64 | auto 路由策略 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-65 | 版本号与 Release Note 管理 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-66 | 版本展示 | 旧规格被后续需求修订；见下节 |
| FR-67 | 多模型管理 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-68 | 模型名称自动获取 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-69 | 侧栏模型选择 | 旧规格被后续需求修订；见下节 |
| FR-70 | 上下文统计 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-71 | 深度研究思考入口 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-72 | 插件面板开关 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-73 | 插件生命周期 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-74 | 插件协议标准输出 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-75 | 默认预装 DSH 插件集 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-76 | 工作空间配置面板 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-77 | 本地与 SSH/SFTP 连接 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-78 | 远程文件与 AI 读取 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-79 | 本地路径 AI 读取修复 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-80 | 文档与缓存配置生效 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-81 | 模型工具闭环 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-82 | 辅助资料来源注册表 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-83 | 多类型来源接入 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-84 | 来源浏览与附件 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-85 | 预留 | 未实现 |
| FR-86 | AI 回复 Markdown 渲染与上下文卡压缩 | 旧规格被后续需求修订；见下节 |
| FR-87 | 策略弹层整合模型选择 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-88 | 任务主题自动总结 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-89 | 文件打开 Markdown 渲染与新标签页视图 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-90 | Token 消耗统计 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-91 | 会话轨迹视图 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-92 | 全局聊天搜索 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-93 | 会话缓存压缩 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-94 | 服务端上下文预算预览与请求快照 | 实现存在；历史验收记录，非本轮全量复测 |
| FR-95 | 专业/经典两排外观与 macOS 布局 | 0.1.6.0 RC4 交付内容；浏览器记录见 UI 设计与发布验收 |
| FR-96 | 设置中的关于与 GitHub 链接 | 0.1.6.0 RC4 交付内容；浏览器记录见 UI 设计与发布验收 |
| FR-97 | 跨客户端开发 Agent 路由 | 0.1.6.0 RC4 交付内容；检查与发布记录见任务账本 |
| FR-98 | 现行文档核对与项目首页 | 0.1.6.0 RC4 交付内容；检查与发布记录见任务账本 |
| FR-99 | 专业工作台定位、场景扩展与版本化 Docker 镜像 | 定制指南、Release 归档、SHA256 和导入启动路径 |
| FR-100 | 环境检查与首次安装 | install.sh 检查/源码/镜像三模式，不覆盖已有配置；安装文档 |

## 3. 明确修订与能力边界

| 项目 | 现行约定 |
| --- | --- |
| FR-03 / FR-88 | 新任务轻量总结主题；失败不阻断任务，保留已有标题 |
| FR-49 / NFR-12 / FR-57 | 颜色语义变量与视觉检查已存在；未完成本轮全组件对比度认证，不写全量 WCAG 达标 |
| FR-52/53/56/60、LIM-15/16/17/21 | 专业/经典 × 浅色/深色/系统；`aide.ui` JSON 持久化 theme/palette；旧 `aide.theme` 迁移；主色变量保留，green.css 覆盖经典色；不再要求只有一个三按钮控件 |
| NFR-16 | 用户本轮明确授权重新设计外观，旧暗色逐像素保真限制不再用于新版布局；经典保留原绿色系 |
| NFR-13 | 无 npm 构建步骤、Go 标准库；允许已随仓库分发的 marked，不能再说完全无第三方组件 |
| NFR-14/15 | 正式发布仍须 embed 资源验证与 CSP 校验；隔离静态预览不是此项完整通过 |
| LIM-18/20 | 原 web 总量 200 KiB 和旧界面测量是当时基线，已不适用新版；颜色变量名作为兼容接口保留。后续新增资源要记录大小而非假报旧阈值通过 |
| FR-66 | 运行版本/commit 来自构建 ldflags，不再从可变工作目录推断镜像版本 |
| FR-69 / FR-87 | 模型选择已并入策略弹层，非独立侧栏模型选择器 |
| FR-86 / FR-89 | Markdown 使用本地 marked + 输出消毒；不是原零库手写解析器，不宣称脚注等全部扩展均支持 |
| FR-81 / LIM-29 | 每个阶段工具循环最多 10 轮，不是 6 轮；文件/命令提案累计预算不可绕过 |
| FR-82~85 / OOS-04 | 多来源可登记/浏览；MCP 仅登记，协议调用未实现；自动系统文档仅自动挂载 |
| FR-93 | 历史超 48,000 UTF-8 字节触发摘要压缩；压缩不是 ZIP，不保证减少磁盘占用 |
| FR-94 | 预算预览与实际请求共用构造器；粗略输入估算与输出预留，超窗口拦截，不能称精确 tokenizer |
| FR-97 / OOS-06 | 开发规范路由不等于产品内多 Agent 自动调度；后者仍未实现 |

其他明确非目标：完整 Cordis 兼容、PTY、向量检索、多用户、跨文件事务、任意模型命令自动执行。UI 上有入口或预设名称不代表这些能力已交付。

## 4. 量化与运行限制

| 限制 | 当前值 / 源码 |
| --- | --- |
| 单文件文本 | 256 KiB，UTF-8、无 NUL；files.go |
| 显式附件 | 最多 8 个，拼装文本最多 80,000 字节；workflow.go |
| 提案 | 最多 10 文件、20 命令、内容总计 512 KiB；workflow.go |
| 工具循环 | 每阶段最多 10 轮；workflow.go |
| 自动压缩阈值 | 48,000 UTF-8 字节；workflow.go |
| 命令 | 60 秒、128 KiB 输出、4 并发；command.go |
| 模型任务 | 每会话一个、全局最多四个、总任务 6 分钟；workflow.go |
| 模型请求 | 单次 120 秒；provider.go |
| 目录 | 单次 2,000 项，无分页；files.go |
| 模型列表 | 最多 20 个；server.go |
| 挂载 | /workspace 与 /local 可写，/context 只读；/local 默认 HOME；compose.yaml |

完整请求字段以 [架构/API 索引](architecture.md) 和当前 handler 为准。权限和原子性限制不因文档整理而放宽。

## 5. 本轮验收与后续

FR-95/96：实际浏览器检查、主题组合持久化、已有前端 VM 回归；证据见 UI 设计记录。FR-97：入口文件、路由自检、失败门禁与受限清理测试；各客户端自动加载须分别握手。FR-98：逐文件登记文档有效性、核对链接与图片、实际渲染首页。

FR-24（流式输出）：实现完成并随本次提交集成源码（见文末追加节）；race 单测、无头 Chromium UI 检查与端到端冒烟通过，真实模型浏览器观感待用户在 8097 确认；未升版、未构建发布镜像。

发布候选还需完成全量测试、正式 embed 构建和发布授权/回滚记录；不得将预览截图当作生产发布通过。

## 6. 文档职责

README 介绍产品，用户指南写操作，HANDOVER 写运维，架构写实现，PRD 登记需求，任务记录写本次进展；历史设计保留原结论并标明被哪一版本取代，避免重复维护“当前状态”。

## 7. 变更记录

- 2026-09-23 v2.0：按当前源码整编，保留 FR-01~93；补登记已存在 FR-94；新增 FR-95~98；纠正 HOME/工具读取/轮数/主题/运行版本等旧描述，历史原文保留。

## 追加：中英文界面与路径配置（源码集成，未发布镜像）

设置 → 语言提供「中文 / English」按钮，即时切换并在当前浏览器保存。仅转换产品界面，不翻译用户输入、文件、会话和模型回复。英文长按钮适配窄侧栏；标志悬停左倾放大，遵循减少动态效果偏好。本地浏览范围由 `.env` 的 `AIDE_LOCAL_ROOT` 配置，新增 `scripts/configure-local-root.py` 辅助设置，修改挂载范围须重建容器。现有 RC4 镜像不自动获得这些源码变更。验收与范围见 [本次记录](reviews/2026-09-23/english-support-verification.md)。

## 启动配置统一（2026-09-24）

正常启动入口为 start.command → scripts/aide.sh → Docker Compose；.env 的 AIDE_PORT 与 COMPOSE_FILE 为端口和挂载组合的唯一配置来源。macOS 共享根模式显式映射 Users、Volumes、private，不能声称完整系统目录均已开放。临时预览不是用户入口。详见 [工作目录配置](workspace-paths.md)。

## 追加：SSE 流式输出与 DSH/Codex 风格显示（2026-09-24，源码集成，未发布镜像）

模型文本响应改为逐 token SSE 推送：新增 `GET /api/sessions/{id}/runs/{run}/events`（step/delta/tool/status/done），`?access_token=` 仅对该路由生效；任务终态仍由会话接口持久化兜底，EventSource 断开时轮询自动降级，运行中取消立即中断流。前端按 DSH/Codex 风格显示：首 token 前呼吸思考点、流式文本+闪烁光标、工具调用实时活动行、多轮工具按轮次重置；delta 渲染按动画帧批量并跳过流式期代码高亮（完成时补全），会话快照未变时跳过整页重渲染；summarizeTopic 并发执行，不再阻塞首 token。配套修复：事件 hub 发送/关闭竞态与晚订阅挂起、流式请求 HTTP 400 时自动去 stream_options 重试一次、EventSource URL 回归检查（进 quick 门禁）；Dockerfile python 基础镜像改为本地 tag 引用（ARG PYTHON_BASE），避免受限网络下按 digest 拉 metadata 卡死启动构建。验收与范围见 [任务账本](tasks/sse-streaming.json)；现有 RC1 镜像不自动获得这些源码变更。

## 追加：排队与插话、发送/停止同键、一键回底（2026-09-24，源码集成，未发布镜像）

任务运行中可继续发送：`queued=true` 进 FIFO 队列、`queued=false` 插话（通道容量 4，满 429 不脏记）；模型每轮返回后先消费插话、再取队首；排队项支持修改/删除/升级插话（`POST /api/sessions/{id}/runs/{run}/queue/{index}`）。界面：队列条参照 Codex pending_input_preview 悬于输入框上方（分区标题 + ↳ 条目 + 提示行）；插话消息带标签入时间线；发送按钮空闲为 ↑、运行中原位切换 ■ 停止（插话/排队经 Enter 或「排队」+Enter）；一键回底按钮 sticky 居中粘于会话区可视底部，滚离约一屏出现、点击平滑回底。验收与范围见 [任务账本](tasks/queue-steer.json)；现有 RC1 镜像不自动获得这些源码变更。

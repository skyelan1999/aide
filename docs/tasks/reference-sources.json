{
  "id": "reference-sources",
  "request": "测试辅助资料接口，补浏览路径按钮并修正异常",
  "changes": [
    "Local/Skill browse button reuses host-mounted directory chooser",
    "FTP/FTPS/SMB username and password fields; unsupported RW disabled and rejected by API",
    "Fix empty localized select options",
    "Link resources preserve exact URL and query instead of parsing HTML as a directory",
    "FTP nested paths, spaces and Unix LIST parsing",
    "SMB file URL and MCP registration-only labels",
    "URL scheme validation and consistent text validation"
  ],
  "verification": [
    "Existing local/Skill/source registry/read-write/disabled/MCP tests pass",
    "Local real HTTP server verifies link read and exact URL",
    "Mock FTP verifies nested directories and filenames with spaces",
    "Browser verifies local chooser, returned path, translated options, FTP credential fields and disabled RW",
    "Docker real services: local/skill/link/ftp/explicit-ftps/sftp/smb1 all 7 passed; AI tool results confirmed in next provider request"
  ],
  "limits": "Verified against isolated Docker real servers. SMB1 file access only; SMB2/3 and MCP execution unsupported. Model provider is scripted to request the files; no paid LLM call was made. External customer servers are not tested.",
  "deployment": "18121 isolated preview updated; production unchanged.",
  "evidence": "docs/reviews/2026-09-23/source-protocol-verification.md"
}
